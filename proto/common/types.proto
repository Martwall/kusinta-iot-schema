syntax = "proto3";

package kusinta.iot.v1.common;

option optimize_for = SPEED;

enum SpaceType {
  SPACE_TYPE_UNSPECIFIED = 0;
  BUILDING = 1;
  FLOOR = 2;
  APARTMENT = 3;
  ROOM = 4;
  COMMON_AREA = 5;
}

enum DeviceOwnershipType {
  DEVICE_OWNERSHIP_TYPE_UNSPECIFIED = 0;
  COMPANY = 1;
  RESIDENT = 2;
}

enum DeviceLifecycleState {
  DEVICE_LIFECYCLE_STATE_UNSPECIFIED = 0;
  PENDING_CLAIM = 1;
  OWNED = 2;
  PENDING_TRANSFER = 3;
  BUILDING_POOL = 4;
}

enum ConnectorTransport {
  CONNECTOR_TRANSPORT_UNSPECIFIED = 0;
  UNIX_SOCKET = 1;
  MTLS_TCP = 2;
}

message ErrorDetail {
  string code = 1;
  string message = 2;
  map<string, string> metadata = 3;
}
