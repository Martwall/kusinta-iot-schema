// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.8
//   protoc               v5.27.3
// source: device/property_update.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import { Timestamp } from "../google/protobuf/timestamp";
import { DeviceId } from "../identity/identity";

export const protobufPackage = "kusinta.iot.v1.device";

/**
 * PropertyUpdate carries a single attribute change from connector to gateway,
 * or from gateway to Flutter app.
 * attribute_name uses PascalCase Matter attribute naming (e.g. "OccupiedHeatingSetpoint").
 * cluster_id_hex is the Matter cluster ID as a 4-char hex string (e.g. "0201" for Thermostat).
 */
export interface PropertyUpdate {
  deviceId?: DeviceId | undefined;
  attributeName?: string | undefined;
  value?:
    | { $case: "intValue"; intValue: number }
    | { $case: "uintValue"; uintValue: number }
    | { $case: "boolValue"; boolValue: boolean }
    | { $case: "floatValue"; floatValue: number }
    | { $case: "stringValue"; stringValue: string }
    | { $case: "bytesValue"; bytesValue: Buffer }
    | undefined;
  timestamp?: Date | undefined;
  clusterIdHex?: string | undefined;
}

export interface PropertyUpdateBatch {
  updates?: PropertyUpdate[] | undefined;
  batchTimestamp?: Date | undefined;
}

function createBasePropertyUpdate(): PropertyUpdate {
  return { deviceId: undefined, attributeName: "", value: undefined, timestamp: undefined, clusterIdHex: "" };
}

export const PropertyUpdate: MessageFns<PropertyUpdate> = {
  encode(message: PropertyUpdate, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.deviceId !== undefined) {
      DeviceId.encode(message.deviceId, writer.uint32(10).fork()).join();
    }
    if (message.attributeName !== undefined && message.attributeName !== "") {
      writer.uint32(18).string(message.attributeName);
    }
    switch (message.value?.$case) {
      case "intValue":
        writer.uint32(24).sint32(message.value.intValue);
        break;
      case "uintValue":
        writer.uint32(32).uint32(message.value.uintValue);
        break;
      case "boolValue":
        writer.uint32(40).bool(message.value.boolValue);
        break;
      case "floatValue":
        writer.uint32(53).float(message.value.floatValue);
        break;
      case "stringValue":
        writer.uint32(58).string(message.value.stringValue);
        break;
      case "bytesValue":
        writer.uint32(66).bytes(message.value.bytesValue);
        break;
    }
    if (message.timestamp !== undefined) {
      Timestamp.encode(toTimestamp(message.timestamp), writer.uint32(74).fork()).join();
    }
    if (message.clusterIdHex !== undefined && message.clusterIdHex !== "") {
      writer.uint32(82).string(message.clusterIdHex);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): PropertyUpdate {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBasePropertyUpdate();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.deviceId = DeviceId.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.attributeName = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.value = { $case: "intValue", intValue: reader.sint32() };
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.value = { $case: "uintValue", uintValue: reader.uint32() };
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.value = { $case: "boolValue", boolValue: reader.bool() };
          continue;
        }
        case 6: {
          if (tag !== 53) {
            break;
          }

          message.value = { $case: "floatValue", floatValue: reader.float() };
          continue;
        }
        case 7: {
          if (tag !== 58) {
            break;
          }

          message.value = { $case: "stringValue", stringValue: reader.string() };
          continue;
        }
        case 8: {
          if (tag !== 66) {
            break;
          }

          message.value = { $case: "bytesValue", bytesValue: Buffer.from(reader.bytes()) };
          continue;
        }
        case 9: {
          if (tag !== 74) {
            break;
          }

          message.timestamp = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
        case 10: {
          if (tag !== 82) {
            break;
          }

          message.clusterIdHex = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): PropertyUpdate {
    return {
      deviceId: isSet(object.deviceId)
        ? DeviceId.fromJSON(object.deviceId)
        : isSet(object.device_id)
        ? DeviceId.fromJSON(object.device_id)
        : undefined,
      attributeName: isSet(object.attributeName)
        ? globalThis.String(object.attributeName)
        : isSet(object.attribute_name)
        ? globalThis.String(object.attribute_name)
        : "",
      value: isSet(object.intValue)
        ? { $case: "intValue", intValue: globalThis.Number(object.intValue) }
        : isSet(object.int_value)
        ? { $case: "intValue", intValue: globalThis.Number(object.int_value) }
        : isSet(object.uintValue)
        ? { $case: "uintValue", uintValue: globalThis.Number(object.uintValue) }
        : isSet(object.uint_value)
        ? { $case: "uintValue", uintValue: globalThis.Number(object.uint_value) }
        : isSet(object.boolValue)
        ? { $case: "boolValue", boolValue: globalThis.Boolean(object.boolValue) }
        : isSet(object.bool_value)
        ? { $case: "boolValue", boolValue: globalThis.Boolean(object.bool_value) }
        : isSet(object.floatValue)
        ? { $case: "floatValue", floatValue: globalThis.Number(object.floatValue) }
        : isSet(object.float_value)
        ? { $case: "floatValue", floatValue: globalThis.Number(object.float_value) }
        : isSet(object.stringValue)
        ? { $case: "stringValue", stringValue: globalThis.String(object.stringValue) }
        : isSet(object.string_value)
        ? { $case: "stringValue", stringValue: globalThis.String(object.string_value) }
        : isSet(object.bytesValue)
        ? { $case: "bytesValue", bytesValue: Buffer.from(bytesFromBase64(object.bytesValue)) }
        : isSet(object.bytes_value)
        ? { $case: "bytesValue", bytesValue: Buffer.from(bytesFromBase64(object.bytes_value)) }
        : undefined,
      timestamp: isSet(object.timestamp) ? fromJsonTimestamp(object.timestamp) : undefined,
      clusterIdHex: isSet(object.clusterIdHex)
        ? globalThis.String(object.clusterIdHex)
        : isSet(object.cluster_id_hex)
        ? globalThis.String(object.cluster_id_hex)
        : "",
    };
  },

  toJSON(message: PropertyUpdate): unknown {
    const obj: any = {};
    if (message.deviceId !== undefined) {
      obj.deviceId = DeviceId.toJSON(message.deviceId);
    }
    if (message.attributeName !== undefined && message.attributeName !== "") {
      obj.attributeName = message.attributeName;
    }
    if (message.value?.$case === "intValue") {
      obj.intValue = Math.round(message.value.intValue);
    } else if (message.value?.$case === "uintValue") {
      obj.uintValue = Math.round(message.value.uintValue);
    } else if (message.value?.$case === "boolValue") {
      obj.boolValue = message.value.boolValue;
    } else if (message.value?.$case === "floatValue") {
      obj.floatValue = message.value.floatValue;
    } else if (message.value?.$case === "stringValue") {
      obj.stringValue = message.value.stringValue;
    } else if (message.value?.$case === "bytesValue") {
      obj.bytesValue = base64FromBytes(message.value.bytesValue);
    }
    if (message.timestamp !== undefined) {
      obj.timestamp = message.timestamp.toISOString();
    }
    if (message.clusterIdHex !== undefined && message.clusterIdHex !== "") {
      obj.clusterIdHex = message.clusterIdHex;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<PropertyUpdate>, I>>(base?: I): PropertyUpdate {
    return PropertyUpdate.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<PropertyUpdate>, I>>(object: I): PropertyUpdate {
    const message = createBasePropertyUpdate();
    message.deviceId = (object.deviceId !== undefined && object.deviceId !== null)
      ? DeviceId.fromPartial(object.deviceId)
      : undefined;
    message.attributeName = object.attributeName ?? "";
    switch (object.value?.$case) {
      case "intValue": {
        if (object.value?.intValue !== undefined && object.value?.intValue !== null) {
          message.value = { $case: "intValue", intValue: object.value.intValue };
        }
        break;
      }
      case "uintValue": {
        if (object.value?.uintValue !== undefined && object.value?.uintValue !== null) {
          message.value = { $case: "uintValue", uintValue: object.value.uintValue };
        }
        break;
      }
      case "boolValue": {
        if (object.value?.boolValue !== undefined && object.value?.boolValue !== null) {
          message.value = { $case: "boolValue", boolValue: object.value.boolValue };
        }
        break;
      }
      case "floatValue": {
        if (object.value?.floatValue !== undefined && object.value?.floatValue !== null) {
          message.value = { $case: "floatValue", floatValue: object.value.floatValue };
        }
        break;
      }
      case "stringValue": {
        if (object.value?.stringValue !== undefined && object.value?.stringValue !== null) {
          message.value = { $case: "stringValue", stringValue: object.value.stringValue };
        }
        break;
      }
      case "bytesValue": {
        if (object.value?.bytesValue !== undefined && object.value?.bytesValue !== null) {
          message.value = { $case: "bytesValue", bytesValue: object.value.bytesValue };
        }
        break;
      }
    }
    message.timestamp = object.timestamp ?? undefined;
    message.clusterIdHex = object.clusterIdHex ?? "";
    return message;
  },
};

function createBasePropertyUpdateBatch(): PropertyUpdateBatch {
  return { updates: [], batchTimestamp: undefined };
}

export const PropertyUpdateBatch: MessageFns<PropertyUpdateBatch> = {
  encode(message: PropertyUpdateBatch, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.updates !== undefined && message.updates.length !== 0) {
      for (const v of message.updates) {
        PropertyUpdate.encode(v!, writer.uint32(10).fork()).join();
      }
    }
    if (message.batchTimestamp !== undefined) {
      Timestamp.encode(toTimestamp(message.batchTimestamp), writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): PropertyUpdateBatch {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBasePropertyUpdateBatch();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          const el = PropertyUpdate.decode(reader, reader.uint32());
          if (el !== undefined) {
            message.updates!.push(el);
          }
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.batchTimestamp = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): PropertyUpdateBatch {
    return {
      updates: globalThis.Array.isArray(object?.updates)
        ? object.updates.map((e: any) => PropertyUpdate.fromJSON(e))
        : [],
      batchTimestamp: isSet(object.batchTimestamp)
        ? fromJsonTimestamp(object.batchTimestamp)
        : isSet(object.batch_timestamp)
        ? fromJsonTimestamp(object.batch_timestamp)
        : undefined,
    };
  },

  toJSON(message: PropertyUpdateBatch): unknown {
    const obj: any = {};
    if (message.updates?.length) {
      obj.updates = message.updates.map((e) => PropertyUpdate.toJSON(e));
    }
    if (message.batchTimestamp !== undefined) {
      obj.batchTimestamp = message.batchTimestamp.toISOString();
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<PropertyUpdateBatch>, I>>(base?: I): PropertyUpdateBatch {
    return PropertyUpdateBatch.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<PropertyUpdateBatch>, I>>(object: I): PropertyUpdateBatch {
    const message = createBasePropertyUpdateBatch();
    message.updates = object.updates?.map((e) => PropertyUpdate.fromPartial(e)) || [];
    message.batchTimestamp = object.batchTimestamp ?? undefined;
    return message;
  },
};

function bytesFromBase64(b64: string): Uint8Array {
  return Uint8Array.from(globalThis.Buffer.from(b64, "base64"));
}

function base64FromBytes(arr: Uint8Array): string {
  return globalThis.Buffer.from(arr).toString("base64");
}

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends { $case: string } ? { [K in keyof Omit<T, "$case">]?: DeepPartial<T[K]> } & { $case: T["$case"] }
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function toTimestamp(date: Date): Timestamp {
  const seconds = Math.trunc(date.getTime() / 1_000);
  const nanos = (date.getTime() % 1_000) * 1_000_000;
  return { seconds, nanos };
}

function fromTimestamp(t: Timestamp): Date {
  let millis = (t.seconds || 0) * 1_000;
  millis += (t.nanos || 0) / 1_000_000;
  return new globalThis.Date(millis);
}

function fromJsonTimestamp(o: any): Date {
  if (o instanceof globalThis.Date) {
    return o;
  } else if (typeof o === "string") {
    return new globalThis.Date(o);
  } else {
    return fromTimestamp(Timestamp.fromJSON(o));
  }
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
