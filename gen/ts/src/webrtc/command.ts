// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.8
//   protoc               v5.27.3
// source: webrtc/command.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import { Timestamp } from "../google/protobuf/timestamp";
import { DeviceId } from "../identity/identity";

export const protobufPackage = "kusinta.iot.v1.webrtc";

/** Maps to Matter SetpointRaiseLower command (Thermostat cluster 0x0201). */
export interface ThermostatSetpointParams {
  /** SetpointAdjustMode: Heat=1, Cool=2, Both=3 */
  mode?:
    | number
    | undefined;
  /** centidegrees delta */
  amount?: number | undefined;
}

/** Maps to Matter MoveToLevel command (Level Control cluster 0x0008). */
export interface LevelControlParams {
  /** 0–254 */
  level?:
    | number
    | undefined;
  /** 100ms units */
  transitionTime?: number | undefined;
}

/** Maps to Matter On and Off commands (On/Off cluster 0x0006). */
export interface OnOffParams {
  on?: boolean | undefined;
  toggle?: boolean | undefined;
}

/** Maps to Matter GoToLiftPercentage command (Window Covering cluster 0x0102). */
export interface WindowCoveringLiftParams {
  /** 0–10000 */
  liftPercent100ths?: number | undefined;
}

/** Maps to Matter LockDoor/UnlockDoor commands (Door Lock cluster 0x0101). */
export interface DoorLockParams {
  /** Locked=1, Unlocked=2 */
  lockState?:
    | number
    | undefined;
  /** optional PIN */
  pinCode?: string | undefined;
}

export interface DeviceCommand {
  /** UUID, matched in CommandResult */
  commandId?: string | undefined;
  deviceId?: DeviceId | undefined;
  clusterIdHex?:
    | string
    | undefined;
  /** PascalCase Matter command name */
  commandName?: string | undefined;
  parameters?:
    | { $case: "thermostatSetpoint"; thermostatSetpoint: ThermostatSetpointParams }
    | { $case: "levelControl"; levelControl: LevelControlParams }
    | { $case: "onOff"; onOff: OnOffParams }
    | { $case: "windowCoveringLift"; windowCoveringLift: WindowCoveringLiftParams }
    | { $case: "doorLock"; doorLock: DoorLockParams }
    | //
    /** escape hatch for unsupported commands */
    { $case: "rawTlv"; rawTlv: Buffer }
    | undefined;
}

export interface CommandError {
  code?: string | undefined;
  message?: string | undefined;
}

/**
 * CommandResult travels gateway → Flutter app (GatewayMessage) and
 * connector → gateway (ConnectorToGatewayMessage via connector.proto import).
 */
export interface CommandResult {
  commandId?: string | undefined;
  success?: boolean | undefined;
  error?: CommandError | undefined;
  completedAt?: Date | undefined;
}

function createBaseThermostatSetpointParams(): ThermostatSetpointParams {
  return { mode: 0, amount: 0 };
}

export const ThermostatSetpointParams: MessageFns<ThermostatSetpointParams> = {
  encode(message: ThermostatSetpointParams, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.mode !== undefined && message.mode !== 0) {
      writer.uint32(8).uint32(message.mode);
    }
    if (message.amount !== undefined && message.amount !== 0) {
      writer.uint32(16).sint32(message.amount);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ThermostatSetpointParams {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseThermostatSetpointParams();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.mode = reader.uint32();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.amount = reader.sint32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ThermostatSetpointParams {
    return {
      mode: isSet(object.mode) ? globalThis.Number(object.mode) : 0,
      amount: isSet(object.amount) ? globalThis.Number(object.amount) : 0,
    };
  },

  toJSON(message: ThermostatSetpointParams): unknown {
    const obj: any = {};
    if (message.mode !== undefined && message.mode !== 0) {
      obj.mode = Math.round(message.mode);
    }
    if (message.amount !== undefined && message.amount !== 0) {
      obj.amount = Math.round(message.amount);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ThermostatSetpointParams>, I>>(base?: I): ThermostatSetpointParams {
    return ThermostatSetpointParams.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ThermostatSetpointParams>, I>>(object: I): ThermostatSetpointParams {
    const message = createBaseThermostatSetpointParams();
    message.mode = object.mode ?? 0;
    message.amount = object.amount ?? 0;
    return message;
  },
};

function createBaseLevelControlParams(): LevelControlParams {
  return { level: 0, transitionTime: 0 };
}

export const LevelControlParams: MessageFns<LevelControlParams> = {
  encode(message: LevelControlParams, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.level !== undefined && message.level !== 0) {
      writer.uint32(8).uint32(message.level);
    }
    if (message.transitionTime !== undefined && message.transitionTime !== 0) {
      writer.uint32(16).uint32(message.transitionTime);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): LevelControlParams {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseLevelControlParams();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.level = reader.uint32();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.transitionTime = reader.uint32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): LevelControlParams {
    return {
      level: isSet(object.level) ? globalThis.Number(object.level) : 0,
      transitionTime: isSet(object.transitionTime)
        ? globalThis.Number(object.transitionTime)
        : isSet(object.transition_time)
        ? globalThis.Number(object.transition_time)
        : 0,
    };
  },

  toJSON(message: LevelControlParams): unknown {
    const obj: any = {};
    if (message.level !== undefined && message.level !== 0) {
      obj.level = Math.round(message.level);
    }
    if (message.transitionTime !== undefined && message.transitionTime !== 0) {
      obj.transitionTime = Math.round(message.transitionTime);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<LevelControlParams>, I>>(base?: I): LevelControlParams {
    return LevelControlParams.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<LevelControlParams>, I>>(object: I): LevelControlParams {
    const message = createBaseLevelControlParams();
    message.level = object.level ?? 0;
    message.transitionTime = object.transitionTime ?? 0;
    return message;
  },
};

function createBaseOnOffParams(): OnOffParams {
  return { on: false, toggle: false };
}

export const OnOffParams: MessageFns<OnOffParams> = {
  encode(message: OnOffParams, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.on !== undefined && message.on !== false) {
      writer.uint32(8).bool(message.on);
    }
    if (message.toggle !== undefined && message.toggle !== false) {
      writer.uint32(16).bool(message.toggle);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): OnOffParams {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseOnOffParams();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.on = reader.bool();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.toggle = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): OnOffParams {
    return {
      on: isSet(object.on) ? globalThis.Boolean(object.on) : false,
      toggle: isSet(object.toggle) ? globalThis.Boolean(object.toggle) : false,
    };
  },

  toJSON(message: OnOffParams): unknown {
    const obj: any = {};
    if (message.on !== undefined && message.on !== false) {
      obj.on = message.on;
    }
    if (message.toggle !== undefined && message.toggle !== false) {
      obj.toggle = message.toggle;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<OnOffParams>, I>>(base?: I): OnOffParams {
    return OnOffParams.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<OnOffParams>, I>>(object: I): OnOffParams {
    const message = createBaseOnOffParams();
    message.on = object.on ?? false;
    message.toggle = object.toggle ?? false;
    return message;
  },
};

function createBaseWindowCoveringLiftParams(): WindowCoveringLiftParams {
  return { liftPercent100ths: 0 };
}

export const WindowCoveringLiftParams: MessageFns<WindowCoveringLiftParams> = {
  encode(message: WindowCoveringLiftParams, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.liftPercent100ths !== undefined && message.liftPercent100ths !== 0) {
      writer.uint32(8).uint32(message.liftPercent100ths);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): WindowCoveringLiftParams {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseWindowCoveringLiftParams();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.liftPercent100ths = reader.uint32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): WindowCoveringLiftParams {
    return {
      liftPercent100ths: isSet(object.liftPercent100ths)
        ? globalThis.Number(object.liftPercent100ths)
        : isSet(object.lift_percent100ths)
        ? globalThis.Number(object.lift_percent100ths)
        : 0,
    };
  },

  toJSON(message: WindowCoveringLiftParams): unknown {
    const obj: any = {};
    if (message.liftPercent100ths !== undefined && message.liftPercent100ths !== 0) {
      obj.liftPercent100ths = Math.round(message.liftPercent100ths);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<WindowCoveringLiftParams>, I>>(base?: I): WindowCoveringLiftParams {
    return WindowCoveringLiftParams.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<WindowCoveringLiftParams>, I>>(object: I): WindowCoveringLiftParams {
    const message = createBaseWindowCoveringLiftParams();
    message.liftPercent100ths = object.liftPercent100ths ?? 0;
    return message;
  },
};

function createBaseDoorLockParams(): DoorLockParams {
  return { lockState: 0, pinCode: "" };
}

export const DoorLockParams: MessageFns<DoorLockParams> = {
  encode(message: DoorLockParams, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.lockState !== undefined && message.lockState !== 0) {
      writer.uint32(8).uint32(message.lockState);
    }
    if (message.pinCode !== undefined && message.pinCode !== "") {
      writer.uint32(18).string(message.pinCode);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): DoorLockParams {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseDoorLockParams();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.lockState = reader.uint32();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.pinCode = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): DoorLockParams {
    return {
      lockState: isSet(object.lockState)
        ? globalThis.Number(object.lockState)
        : isSet(object.lock_state)
        ? globalThis.Number(object.lock_state)
        : 0,
      pinCode: isSet(object.pinCode)
        ? globalThis.String(object.pinCode)
        : isSet(object.pin_code)
        ? globalThis.String(object.pin_code)
        : "",
    };
  },

  toJSON(message: DoorLockParams): unknown {
    const obj: any = {};
    if (message.lockState !== undefined && message.lockState !== 0) {
      obj.lockState = Math.round(message.lockState);
    }
    if (message.pinCode !== undefined && message.pinCode !== "") {
      obj.pinCode = message.pinCode;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<DoorLockParams>, I>>(base?: I): DoorLockParams {
    return DoorLockParams.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<DoorLockParams>, I>>(object: I): DoorLockParams {
    const message = createBaseDoorLockParams();
    message.lockState = object.lockState ?? 0;
    message.pinCode = object.pinCode ?? "";
    return message;
  },
};

function createBaseDeviceCommand(): DeviceCommand {
  return { commandId: "", deviceId: undefined, clusterIdHex: "", commandName: "", parameters: undefined };
}

export const DeviceCommand: MessageFns<DeviceCommand> = {
  encode(message: DeviceCommand, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.commandId !== undefined && message.commandId !== "") {
      writer.uint32(10).string(message.commandId);
    }
    if (message.deviceId !== undefined) {
      DeviceId.encode(message.deviceId, writer.uint32(18).fork()).join();
    }
    if (message.clusterIdHex !== undefined && message.clusterIdHex !== "") {
      writer.uint32(26).string(message.clusterIdHex);
    }
    if (message.commandName !== undefined && message.commandName !== "") {
      writer.uint32(34).string(message.commandName);
    }
    switch (message.parameters?.$case) {
      case "thermostatSetpoint":
        ThermostatSetpointParams.encode(message.parameters.thermostatSetpoint, writer.uint32(42).fork()).join();
        break;
      case "levelControl":
        LevelControlParams.encode(message.parameters.levelControl, writer.uint32(50).fork()).join();
        break;
      case "onOff":
        OnOffParams.encode(message.parameters.onOff, writer.uint32(58).fork()).join();
        break;
      case "windowCoveringLift":
        WindowCoveringLiftParams.encode(message.parameters.windowCoveringLift, writer.uint32(66).fork()).join();
        break;
      case "doorLock":
        DoorLockParams.encode(message.parameters.doorLock, writer.uint32(74).fork()).join();
        break;
      case "rawTlv":
        writer.uint32(794).bytes(message.parameters.rawTlv);
        break;
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): DeviceCommand {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseDeviceCommand();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.commandId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.deviceId = DeviceId.decode(reader, reader.uint32());
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.clusterIdHex = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.commandName = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.parameters = {
            $case: "thermostatSetpoint",
            thermostatSetpoint: ThermostatSetpointParams.decode(reader, reader.uint32()),
          };
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.parameters = {
            $case: "levelControl",
            levelControl: LevelControlParams.decode(reader, reader.uint32()),
          };
          continue;
        }
        case 7: {
          if (tag !== 58) {
            break;
          }

          message.parameters = { $case: "onOff", onOff: OnOffParams.decode(reader, reader.uint32()) };
          continue;
        }
        case 8: {
          if (tag !== 66) {
            break;
          }

          message.parameters = {
            $case: "windowCoveringLift",
            windowCoveringLift: WindowCoveringLiftParams.decode(reader, reader.uint32()),
          };
          continue;
        }
        case 9: {
          if (tag !== 74) {
            break;
          }

          message.parameters = { $case: "doorLock", doorLock: DoorLockParams.decode(reader, reader.uint32()) };
          continue;
        }
        case 99: {
          if (tag !== 794) {
            break;
          }

          message.parameters = { $case: "rawTlv", rawTlv: Buffer.from(reader.bytes()) };
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): DeviceCommand {
    return {
      commandId: isSet(object.commandId)
        ? globalThis.String(object.commandId)
        : isSet(object.command_id)
        ? globalThis.String(object.command_id)
        : "",
      deviceId: isSet(object.deviceId)
        ? DeviceId.fromJSON(object.deviceId)
        : isSet(object.device_id)
        ? DeviceId.fromJSON(object.device_id)
        : undefined,
      clusterIdHex: isSet(object.clusterIdHex)
        ? globalThis.String(object.clusterIdHex)
        : isSet(object.cluster_id_hex)
        ? globalThis.String(object.cluster_id_hex)
        : "",
      commandName: isSet(object.commandName)
        ? globalThis.String(object.commandName)
        : isSet(object.command_name)
        ? globalThis.String(object.command_name)
        : "",
      parameters: isSet(object.thermostatSetpoint)
        ? {
          $case: "thermostatSetpoint",
          thermostatSetpoint: ThermostatSetpointParams.fromJSON(object.thermostatSetpoint),
        }
        : isSet(object.thermostat_setpoint)
        ? {
          $case: "thermostatSetpoint",
          thermostatSetpoint: ThermostatSetpointParams.fromJSON(object.thermostat_setpoint),
        }
        : isSet(object.levelControl)
        ? { $case: "levelControl", levelControl: LevelControlParams.fromJSON(object.levelControl) }
        : isSet(object.level_control)
        ? { $case: "levelControl", levelControl: LevelControlParams.fromJSON(object.level_control) }
        : isSet(object.onOff)
        ? { $case: "onOff", onOff: OnOffParams.fromJSON(object.onOff) }
        : isSet(object.on_off)
        ? { $case: "onOff", onOff: OnOffParams.fromJSON(object.on_off) }
        : isSet(object.windowCoveringLift)
        ? {
          $case: "windowCoveringLift",
          windowCoveringLift: WindowCoveringLiftParams.fromJSON(object.windowCoveringLift),
        }
        : isSet(object.window_covering_lift)
        ? {
          $case: "windowCoveringLift",
          windowCoveringLift: WindowCoveringLiftParams.fromJSON(object.window_covering_lift),
        }
        : isSet(object.doorLock)
        ? { $case: "doorLock", doorLock: DoorLockParams.fromJSON(object.doorLock) }
        : isSet(object.door_lock)
        ? { $case: "doorLock", doorLock: DoorLockParams.fromJSON(object.door_lock) }
        : isSet(object.rawTlv)
        ? { $case: "rawTlv", rawTlv: Buffer.from(bytesFromBase64(object.rawTlv)) }
        : isSet(object.raw_tlv)
        ? { $case: "rawTlv", rawTlv: Buffer.from(bytesFromBase64(object.raw_tlv)) }
        : undefined,
    };
  },

  toJSON(message: DeviceCommand): unknown {
    const obj: any = {};
    if (message.commandId !== undefined && message.commandId !== "") {
      obj.commandId = message.commandId;
    }
    if (message.deviceId !== undefined) {
      obj.deviceId = DeviceId.toJSON(message.deviceId);
    }
    if (message.clusterIdHex !== undefined && message.clusterIdHex !== "") {
      obj.clusterIdHex = message.clusterIdHex;
    }
    if (message.commandName !== undefined && message.commandName !== "") {
      obj.commandName = message.commandName;
    }
    if (message.parameters?.$case === "thermostatSetpoint") {
      obj.thermostatSetpoint = ThermostatSetpointParams.toJSON(message.parameters.thermostatSetpoint);
    } else if (message.parameters?.$case === "levelControl") {
      obj.levelControl = LevelControlParams.toJSON(message.parameters.levelControl);
    } else if (message.parameters?.$case === "onOff") {
      obj.onOff = OnOffParams.toJSON(message.parameters.onOff);
    } else if (message.parameters?.$case === "windowCoveringLift") {
      obj.windowCoveringLift = WindowCoveringLiftParams.toJSON(message.parameters.windowCoveringLift);
    } else if (message.parameters?.$case === "doorLock") {
      obj.doorLock = DoorLockParams.toJSON(message.parameters.doorLock);
    } else if (message.parameters?.$case === "rawTlv") {
      obj.rawTlv = base64FromBytes(message.parameters.rawTlv);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<DeviceCommand>, I>>(base?: I): DeviceCommand {
    return DeviceCommand.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<DeviceCommand>, I>>(object: I): DeviceCommand {
    const message = createBaseDeviceCommand();
    message.commandId = object.commandId ?? "";
    message.deviceId = (object.deviceId !== undefined && object.deviceId !== null)
      ? DeviceId.fromPartial(object.deviceId)
      : undefined;
    message.clusterIdHex = object.clusterIdHex ?? "";
    message.commandName = object.commandName ?? "";
    switch (object.parameters?.$case) {
      case "thermostatSetpoint": {
        if (object.parameters?.thermostatSetpoint !== undefined && object.parameters?.thermostatSetpoint !== null) {
          message.parameters = {
            $case: "thermostatSetpoint",
            thermostatSetpoint: ThermostatSetpointParams.fromPartial(object.parameters.thermostatSetpoint),
          };
        }
        break;
      }
      case "levelControl": {
        if (object.parameters?.levelControl !== undefined && object.parameters?.levelControl !== null) {
          message.parameters = {
            $case: "levelControl",
            levelControl: LevelControlParams.fromPartial(object.parameters.levelControl),
          };
        }
        break;
      }
      case "onOff": {
        if (object.parameters?.onOff !== undefined && object.parameters?.onOff !== null) {
          message.parameters = { $case: "onOff", onOff: OnOffParams.fromPartial(object.parameters.onOff) };
        }
        break;
      }
      case "windowCoveringLift": {
        if (object.parameters?.windowCoveringLift !== undefined && object.parameters?.windowCoveringLift !== null) {
          message.parameters = {
            $case: "windowCoveringLift",
            windowCoveringLift: WindowCoveringLiftParams.fromPartial(object.parameters.windowCoveringLift),
          };
        }
        break;
      }
      case "doorLock": {
        if (object.parameters?.doorLock !== undefined && object.parameters?.doorLock !== null) {
          message.parameters = { $case: "doorLock", doorLock: DoorLockParams.fromPartial(object.parameters.doorLock) };
        }
        break;
      }
      case "rawTlv": {
        if (object.parameters?.rawTlv !== undefined && object.parameters?.rawTlv !== null) {
          message.parameters = { $case: "rawTlv", rawTlv: object.parameters.rawTlv };
        }
        break;
      }
    }
    return message;
  },
};

function createBaseCommandError(): CommandError {
  return { code: "", message: "" };
}

export const CommandError: MessageFns<CommandError> = {
  encode(message: CommandError, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.code !== undefined && message.code !== "") {
      writer.uint32(10).string(message.code);
    }
    if (message.message !== undefined && message.message !== "") {
      writer.uint32(18).string(message.message);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): CommandError {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseCommandError();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.code = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.message = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): CommandError {
    return {
      code: isSet(object.code) ? globalThis.String(object.code) : "",
      message: isSet(object.message) ? globalThis.String(object.message) : "",
    };
  },

  toJSON(message: CommandError): unknown {
    const obj: any = {};
    if (message.code !== undefined && message.code !== "") {
      obj.code = message.code;
    }
    if (message.message !== undefined && message.message !== "") {
      obj.message = message.message;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<CommandError>, I>>(base?: I): CommandError {
    return CommandError.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<CommandError>, I>>(object: I): CommandError {
    const message = createBaseCommandError();
    message.code = object.code ?? "";
    message.message = object.message ?? "";
    return message;
  },
};

function createBaseCommandResult(): CommandResult {
  return { commandId: "", success: false, error: undefined, completedAt: undefined };
}

export const CommandResult: MessageFns<CommandResult> = {
  encode(message: CommandResult, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.commandId !== undefined && message.commandId !== "") {
      writer.uint32(10).string(message.commandId);
    }
    if (message.success !== undefined && message.success !== false) {
      writer.uint32(16).bool(message.success);
    }
    if (message.error !== undefined) {
      CommandError.encode(message.error, writer.uint32(26).fork()).join();
    }
    if (message.completedAt !== undefined) {
      Timestamp.encode(toTimestamp(message.completedAt), writer.uint32(34).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): CommandResult {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseCommandResult();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.commandId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.success = reader.bool();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.error = CommandError.decode(reader, reader.uint32());
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.completedAt = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): CommandResult {
    return {
      commandId: isSet(object.commandId)
        ? globalThis.String(object.commandId)
        : isSet(object.command_id)
        ? globalThis.String(object.command_id)
        : "",
      success: isSet(object.success) ? globalThis.Boolean(object.success) : false,
      error: isSet(object.error) ? CommandError.fromJSON(object.error) : undefined,
      completedAt: isSet(object.completedAt)
        ? fromJsonTimestamp(object.completedAt)
        : isSet(object.completed_at)
        ? fromJsonTimestamp(object.completed_at)
        : undefined,
    };
  },

  toJSON(message: CommandResult): unknown {
    const obj: any = {};
    if (message.commandId !== undefined && message.commandId !== "") {
      obj.commandId = message.commandId;
    }
    if (message.success !== undefined && message.success !== false) {
      obj.success = message.success;
    }
    if (message.error !== undefined) {
      obj.error = CommandError.toJSON(message.error);
    }
    if (message.completedAt !== undefined) {
      obj.completedAt = message.completedAt.toISOString();
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<CommandResult>, I>>(base?: I): CommandResult {
    return CommandResult.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<CommandResult>, I>>(object: I): CommandResult {
    const message = createBaseCommandResult();
    message.commandId = object.commandId ?? "";
    message.success = object.success ?? false;
    message.error = (object.error !== undefined && object.error !== null)
      ? CommandError.fromPartial(object.error)
      : undefined;
    message.completedAt = object.completedAt ?? undefined;
    return message;
  },
};

function bytesFromBase64(b64: string): Uint8Array {
  return Uint8Array.from(globalThis.Buffer.from(b64, "base64"));
}

function base64FromBytes(arr: Uint8Array): string {
  return globalThis.Buffer.from(arr).toString("base64");
}

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends { $case: string } ? { [K in keyof Omit<T, "$case">]?: DeepPartial<T[K]> } & { $case: T["$case"] }
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function toTimestamp(date: Date): Timestamp {
  const seconds = Math.trunc(date.getTime() / 1_000);
  const nanos = (date.getTime() % 1_000) * 1_000_000;
  return { seconds, nanos };
}

function fromTimestamp(t: Timestamp): Date {
  let millis = (t.seconds || 0) * 1_000;
  millis += (t.nanos || 0) / 1_000_000;
  return new globalThis.Date(millis);
}

function fromJsonTimestamp(o: any): Date {
  if (o instanceof globalThis.Date) {
    return o;
  } else if (typeof o === "string") {
    return new globalThis.Date(o);
  } else {
    return fromTimestamp(Timestamp.fromJSON(o));
  }
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
