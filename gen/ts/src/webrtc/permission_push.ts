// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.8
//   protoc               v5.27.3
// source: webrtc/permission_push.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import { EffectivePermissions } from "../access/acl";
import { DeviceId } from "../identity/identity";

export const protobufPackage = "kusinta.iot.v1.webrtc";

/**
 * Pushed to the Flutter app whenever the user's effective permissions change.
 * change_reason values: "DEVICE_ASSIGNED", "DEVICE_UNASSIGNED", "RESIDENT_CHANGED", "CONSTRAINT_UPDATED"
 */
export interface LivePermissionUpdate {
  newPermissions?: EffectivePermissions | undefined;
  addedDevices?: DeviceId[] | undefined;
  removedDevices?: DeviceId[] | undefined;
  changeReason?: string | undefined;
}

function createBaseLivePermissionUpdate(): LivePermissionUpdate {
  return { newPermissions: undefined, addedDevices: [], removedDevices: [], changeReason: "" };
}

export const LivePermissionUpdate: MessageFns<LivePermissionUpdate> = {
  encode(message: LivePermissionUpdate, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.newPermissions !== undefined) {
      EffectivePermissions.encode(message.newPermissions, writer.uint32(10).fork()).join();
    }
    if (message.addedDevices !== undefined && message.addedDevices.length !== 0) {
      for (const v of message.addedDevices) {
        DeviceId.encode(v!, writer.uint32(18).fork()).join();
      }
    }
    if (message.removedDevices !== undefined && message.removedDevices.length !== 0) {
      for (const v of message.removedDevices) {
        DeviceId.encode(v!, writer.uint32(26).fork()).join();
      }
    }
    if (message.changeReason !== undefined && message.changeReason !== "") {
      writer.uint32(34).string(message.changeReason);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): LivePermissionUpdate {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseLivePermissionUpdate();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.newPermissions = EffectivePermissions.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          const el = DeviceId.decode(reader, reader.uint32());
          if (el !== undefined) {
            message.addedDevices!.push(el);
          }
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          const el = DeviceId.decode(reader, reader.uint32());
          if (el !== undefined) {
            message.removedDevices!.push(el);
          }
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.changeReason = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): LivePermissionUpdate {
    return {
      newPermissions: isSet(object.newPermissions)
        ? EffectivePermissions.fromJSON(object.newPermissions)
        : isSet(object.new_permissions)
        ? EffectivePermissions.fromJSON(object.new_permissions)
        : undefined,
      addedDevices: globalThis.Array.isArray(object?.addedDevices)
        ? object.addedDevices.map((e: any) => DeviceId.fromJSON(e))
        : globalThis.Array.isArray(object?.added_devices)
        ? object.added_devices.map((e: any) => DeviceId.fromJSON(e))
        : [],
      removedDevices: globalThis.Array.isArray(object?.removedDevices)
        ? object.removedDevices.map((e: any) => DeviceId.fromJSON(e))
        : globalThis.Array.isArray(object?.removed_devices)
        ? object.removed_devices.map((e: any) => DeviceId.fromJSON(e))
        : [],
      changeReason: isSet(object.changeReason)
        ? globalThis.String(object.changeReason)
        : isSet(object.change_reason)
        ? globalThis.String(object.change_reason)
        : "",
    };
  },

  toJSON(message: LivePermissionUpdate): unknown {
    const obj: any = {};
    if (message.newPermissions !== undefined) {
      obj.newPermissions = EffectivePermissions.toJSON(message.newPermissions);
    }
    if (message.addedDevices?.length) {
      obj.addedDevices = message.addedDevices.map((e) => DeviceId.toJSON(e));
    }
    if (message.removedDevices?.length) {
      obj.removedDevices = message.removedDevices.map((e) => DeviceId.toJSON(e));
    }
    if (message.changeReason !== undefined && message.changeReason !== "") {
      obj.changeReason = message.changeReason;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<LivePermissionUpdate>, I>>(base?: I): LivePermissionUpdate {
    return LivePermissionUpdate.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<LivePermissionUpdate>, I>>(object: I): LivePermissionUpdate {
    const message = createBaseLivePermissionUpdate();
    message.newPermissions = (object.newPermissions !== undefined && object.newPermissions !== null)
      ? EffectivePermissions.fromPartial(object.newPermissions)
      : undefined;
    message.addedDevices = object.addedDevices?.map((e) => DeviceId.fromPartial(e)) || [];
    message.removedDevices = object.removedDevices?.map((e) => DeviceId.fromPartial(e)) || [];
    message.changeReason = object.changeReason ?? "";
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends { $case: string } ? { [K in keyof Omit<T, "$case">]?: DeepPartial<T[K]> } & { $case: T["$case"] }
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
