// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.8
//   protoc               v5.27.3
// source: webrtc/envelope.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import { Timestamp } from "../google/protobuf/timestamp";
import { DeviceId } from "../identity/identity";
import { CommandResult, DeviceCommand } from "./command";
import { DevicePropertyEvent, DeviceStateSnapshot } from "./device_state";
import { LivePermissionUpdate } from "./permission_push";

export const protobufPackage = "kusinta.iot.v1.webrtc";

export interface Ping {
}

export interface Pong {
}

/**
 * AppHandshake must be the first message sent by the Flutter app on the data channel.
 * The gateway validates the JWT locally using the mykusinta-api-server public key,
 * checks that gateway_id in the JWT matches this gateway, then extracts the role.
 */
export interface AppHandshake {
  jwt?: string | undefined;
  subscribeDeviceIds?: DeviceId[] | undefined;
}

export interface PropertyReadRequest {
  deviceId?: DeviceId | undefined;
  attributeName?: string | undefined;
  clusterIdHex?: string | undefined;
}

/** GatewayMessage: gateway → Flutter app */
export interface GatewayMessage {
  messageId?: string | undefined;
  sentAt?: Date | undefined;
  payload?:
    | { $case: "stateSnapshot"; stateSnapshot: DeviceStateSnapshot }
    | { $case: "propertyEvent"; propertyEvent: DevicePropertyEvent }
    | { $case: "permissionUpdate"; permissionUpdate: LivePermissionUpdate }
    | { $case: "commandResult"; commandResult: CommandResult }
    | //
    /** error description string */
    { $case: "error"; error: string }
    | { $case: "pong"; pong: Pong }
    | undefined;
}

/** AppMessage: Flutter app → gateway */
export interface AppMessage {
  messageId?: string | undefined;
  sentAt?: Date | undefined;
  payload?:
    | { $case: "handshake"; handshake: AppHandshake }
    | { $case: "command"; command: DeviceCommand }
    | { $case: "readRequest"; readRequest: PropertyReadRequest }
    | { $case: "ping"; ping: Ping }
    | undefined;
}

function createBasePing(): Ping {
  return {};
}

export const Ping: MessageFns<Ping> = {
  encode(_: Ping, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Ping {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBasePing();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(_: any): Ping {
    return {};
  },

  toJSON(_: Ping): unknown {
    const obj: any = {};
    return obj;
  },

  create<I extends Exact<DeepPartial<Ping>, I>>(base?: I): Ping {
    return Ping.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<Ping>, I>>(_: I): Ping {
    const message = createBasePing();
    return message;
  },
};

function createBasePong(): Pong {
  return {};
}

export const Pong: MessageFns<Pong> = {
  encode(_: Pong, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Pong {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBasePong();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(_: any): Pong {
    return {};
  },

  toJSON(_: Pong): unknown {
    const obj: any = {};
    return obj;
  },

  create<I extends Exact<DeepPartial<Pong>, I>>(base?: I): Pong {
    return Pong.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<Pong>, I>>(_: I): Pong {
    const message = createBasePong();
    return message;
  },
};

function createBaseAppHandshake(): AppHandshake {
  return { jwt: "", subscribeDeviceIds: [] };
}

export const AppHandshake: MessageFns<AppHandshake> = {
  encode(message: AppHandshake, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.jwt !== undefined && message.jwt !== "") {
      writer.uint32(10).string(message.jwt);
    }
    if (message.subscribeDeviceIds !== undefined && message.subscribeDeviceIds.length !== 0) {
      for (const v of message.subscribeDeviceIds) {
        DeviceId.encode(v!, writer.uint32(18).fork()).join();
      }
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): AppHandshake {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseAppHandshake();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.jwt = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          const el = DeviceId.decode(reader, reader.uint32());
          if (el !== undefined) {
            message.subscribeDeviceIds!.push(el);
          }
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): AppHandshake {
    return {
      jwt: isSet(object.jwt) ? globalThis.String(object.jwt) : "",
      subscribeDeviceIds: globalThis.Array.isArray(object?.subscribeDeviceIds)
        ? object.subscribeDeviceIds.map((e: any) => DeviceId.fromJSON(e))
        : globalThis.Array.isArray(object?.subscribe_device_ids)
        ? object.subscribe_device_ids.map((e: any) => DeviceId.fromJSON(e))
        : [],
    };
  },

  toJSON(message: AppHandshake): unknown {
    const obj: any = {};
    if (message.jwt !== undefined && message.jwt !== "") {
      obj.jwt = message.jwt;
    }
    if (message.subscribeDeviceIds?.length) {
      obj.subscribeDeviceIds = message.subscribeDeviceIds.map((e) => DeviceId.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<AppHandshake>, I>>(base?: I): AppHandshake {
    return AppHandshake.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<AppHandshake>, I>>(object: I): AppHandshake {
    const message = createBaseAppHandshake();
    message.jwt = object.jwt ?? "";
    message.subscribeDeviceIds = object.subscribeDeviceIds?.map((e) => DeviceId.fromPartial(e)) || [];
    return message;
  },
};

function createBasePropertyReadRequest(): PropertyReadRequest {
  return { deviceId: undefined, attributeName: "", clusterIdHex: "" };
}

export const PropertyReadRequest: MessageFns<PropertyReadRequest> = {
  encode(message: PropertyReadRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.deviceId !== undefined) {
      DeviceId.encode(message.deviceId, writer.uint32(10).fork()).join();
    }
    if (message.attributeName !== undefined && message.attributeName !== "") {
      writer.uint32(18).string(message.attributeName);
    }
    if (message.clusterIdHex !== undefined && message.clusterIdHex !== "") {
      writer.uint32(26).string(message.clusterIdHex);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): PropertyReadRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBasePropertyReadRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.deviceId = DeviceId.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.attributeName = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.clusterIdHex = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): PropertyReadRequest {
    return {
      deviceId: isSet(object.deviceId)
        ? DeviceId.fromJSON(object.deviceId)
        : isSet(object.device_id)
        ? DeviceId.fromJSON(object.device_id)
        : undefined,
      attributeName: isSet(object.attributeName)
        ? globalThis.String(object.attributeName)
        : isSet(object.attribute_name)
        ? globalThis.String(object.attribute_name)
        : "",
      clusterIdHex: isSet(object.clusterIdHex)
        ? globalThis.String(object.clusterIdHex)
        : isSet(object.cluster_id_hex)
        ? globalThis.String(object.cluster_id_hex)
        : "",
    };
  },

  toJSON(message: PropertyReadRequest): unknown {
    const obj: any = {};
    if (message.deviceId !== undefined) {
      obj.deviceId = DeviceId.toJSON(message.deviceId);
    }
    if (message.attributeName !== undefined && message.attributeName !== "") {
      obj.attributeName = message.attributeName;
    }
    if (message.clusterIdHex !== undefined && message.clusterIdHex !== "") {
      obj.clusterIdHex = message.clusterIdHex;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<PropertyReadRequest>, I>>(base?: I): PropertyReadRequest {
    return PropertyReadRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<PropertyReadRequest>, I>>(object: I): PropertyReadRequest {
    const message = createBasePropertyReadRequest();
    message.deviceId = (object.deviceId !== undefined && object.deviceId !== null)
      ? DeviceId.fromPartial(object.deviceId)
      : undefined;
    message.attributeName = object.attributeName ?? "";
    message.clusterIdHex = object.clusterIdHex ?? "";
    return message;
  },
};

function createBaseGatewayMessage(): GatewayMessage {
  return { messageId: "", sentAt: undefined, payload: undefined };
}

export const GatewayMessage: MessageFns<GatewayMessage> = {
  encode(message: GatewayMessage, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.messageId !== undefined && message.messageId !== "") {
      writer.uint32(10).string(message.messageId);
    }
    if (message.sentAt !== undefined) {
      Timestamp.encode(toTimestamp(message.sentAt), writer.uint32(18).fork()).join();
    }
    switch (message.payload?.$case) {
      case "stateSnapshot":
        DeviceStateSnapshot.encode(message.payload.stateSnapshot, writer.uint32(26).fork()).join();
        break;
      case "propertyEvent":
        DevicePropertyEvent.encode(message.payload.propertyEvent, writer.uint32(34).fork()).join();
        break;
      case "permissionUpdate":
        LivePermissionUpdate.encode(message.payload.permissionUpdate, writer.uint32(42).fork()).join();
        break;
      case "commandResult":
        CommandResult.encode(message.payload.commandResult, writer.uint32(50).fork()).join();
        break;
      case "error":
        writer.uint32(58).string(message.payload.error);
        break;
      case "pong":
        Pong.encode(message.payload.pong, writer.uint32(66).fork()).join();
        break;
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GatewayMessage {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGatewayMessage();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.messageId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.sentAt = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.payload = {
            $case: "stateSnapshot",
            stateSnapshot: DeviceStateSnapshot.decode(reader, reader.uint32()),
          };
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.payload = {
            $case: "propertyEvent",
            propertyEvent: DevicePropertyEvent.decode(reader, reader.uint32()),
          };
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.payload = {
            $case: "permissionUpdate",
            permissionUpdate: LivePermissionUpdate.decode(reader, reader.uint32()),
          };
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.payload = { $case: "commandResult", commandResult: CommandResult.decode(reader, reader.uint32()) };
          continue;
        }
        case 7: {
          if (tag !== 58) {
            break;
          }

          message.payload = { $case: "error", error: reader.string() };
          continue;
        }
        case 8: {
          if (tag !== 66) {
            break;
          }

          message.payload = { $case: "pong", pong: Pong.decode(reader, reader.uint32()) };
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GatewayMessage {
    return {
      messageId: isSet(object.messageId)
        ? globalThis.String(object.messageId)
        : isSet(object.message_id)
        ? globalThis.String(object.message_id)
        : "",
      sentAt: isSet(object.sentAt)
        ? fromJsonTimestamp(object.sentAt)
        : isSet(object.sent_at)
        ? fromJsonTimestamp(object.sent_at)
        : undefined,
      payload: isSet(object.stateSnapshot)
        ? { $case: "stateSnapshot", stateSnapshot: DeviceStateSnapshot.fromJSON(object.stateSnapshot) }
        : isSet(object.state_snapshot)
        ? { $case: "stateSnapshot", stateSnapshot: DeviceStateSnapshot.fromJSON(object.state_snapshot) }
        : isSet(object.propertyEvent)
        ? { $case: "propertyEvent", propertyEvent: DevicePropertyEvent.fromJSON(object.propertyEvent) }
        : isSet(object.property_event)
        ? { $case: "propertyEvent", propertyEvent: DevicePropertyEvent.fromJSON(object.property_event) }
        : isSet(object.permissionUpdate)
        ? { $case: "permissionUpdate", permissionUpdate: LivePermissionUpdate.fromJSON(object.permissionUpdate) }
        : isSet(object.permission_update)
        ? { $case: "permissionUpdate", permissionUpdate: LivePermissionUpdate.fromJSON(object.permission_update) }
        : isSet(object.commandResult)
        ? { $case: "commandResult", commandResult: CommandResult.fromJSON(object.commandResult) }
        : isSet(object.command_result)
        ? { $case: "commandResult", commandResult: CommandResult.fromJSON(object.command_result) }
        : isSet(object.error)
        ? { $case: "error", error: globalThis.String(object.error) }
        : isSet(object.pong)
        ? { $case: "pong", pong: Pong.fromJSON(object.pong) }
        : undefined,
    };
  },

  toJSON(message: GatewayMessage): unknown {
    const obj: any = {};
    if (message.messageId !== undefined && message.messageId !== "") {
      obj.messageId = message.messageId;
    }
    if (message.sentAt !== undefined) {
      obj.sentAt = message.sentAt.toISOString();
    }
    if (message.payload?.$case === "stateSnapshot") {
      obj.stateSnapshot = DeviceStateSnapshot.toJSON(message.payload.stateSnapshot);
    } else if (message.payload?.$case === "propertyEvent") {
      obj.propertyEvent = DevicePropertyEvent.toJSON(message.payload.propertyEvent);
    } else if (message.payload?.$case === "permissionUpdate") {
      obj.permissionUpdate = LivePermissionUpdate.toJSON(message.payload.permissionUpdate);
    } else if (message.payload?.$case === "commandResult") {
      obj.commandResult = CommandResult.toJSON(message.payload.commandResult);
    } else if (message.payload?.$case === "error") {
      obj.error = message.payload.error;
    } else if (message.payload?.$case === "pong") {
      obj.pong = Pong.toJSON(message.payload.pong);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GatewayMessage>, I>>(base?: I): GatewayMessage {
    return GatewayMessage.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GatewayMessage>, I>>(object: I): GatewayMessage {
    const message = createBaseGatewayMessage();
    message.messageId = object.messageId ?? "";
    message.sentAt = object.sentAt ?? undefined;
    switch (object.payload?.$case) {
      case "stateSnapshot": {
        if (object.payload?.stateSnapshot !== undefined && object.payload?.stateSnapshot !== null) {
          message.payload = {
            $case: "stateSnapshot",
            stateSnapshot: DeviceStateSnapshot.fromPartial(object.payload.stateSnapshot),
          };
        }
        break;
      }
      case "propertyEvent": {
        if (object.payload?.propertyEvent !== undefined && object.payload?.propertyEvent !== null) {
          message.payload = {
            $case: "propertyEvent",
            propertyEvent: DevicePropertyEvent.fromPartial(object.payload.propertyEvent),
          };
        }
        break;
      }
      case "permissionUpdate": {
        if (object.payload?.permissionUpdate !== undefined && object.payload?.permissionUpdate !== null) {
          message.payload = {
            $case: "permissionUpdate",
            permissionUpdate: LivePermissionUpdate.fromPartial(object.payload.permissionUpdate),
          };
        }
        break;
      }
      case "commandResult": {
        if (object.payload?.commandResult !== undefined && object.payload?.commandResult !== null) {
          message.payload = {
            $case: "commandResult",
            commandResult: CommandResult.fromPartial(object.payload.commandResult),
          };
        }
        break;
      }
      case "error": {
        if (object.payload?.error !== undefined && object.payload?.error !== null) {
          message.payload = { $case: "error", error: object.payload.error };
        }
        break;
      }
      case "pong": {
        if (object.payload?.pong !== undefined && object.payload?.pong !== null) {
          message.payload = { $case: "pong", pong: Pong.fromPartial(object.payload.pong) };
        }
        break;
      }
    }
    return message;
  },
};

function createBaseAppMessage(): AppMessage {
  return { messageId: "", sentAt: undefined, payload: undefined };
}

export const AppMessage: MessageFns<AppMessage> = {
  encode(message: AppMessage, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.messageId !== undefined && message.messageId !== "") {
      writer.uint32(10).string(message.messageId);
    }
    if (message.sentAt !== undefined) {
      Timestamp.encode(toTimestamp(message.sentAt), writer.uint32(18).fork()).join();
    }
    switch (message.payload?.$case) {
      case "handshake":
        AppHandshake.encode(message.payload.handshake, writer.uint32(26).fork()).join();
        break;
      case "command":
        DeviceCommand.encode(message.payload.command, writer.uint32(34).fork()).join();
        break;
      case "readRequest":
        PropertyReadRequest.encode(message.payload.readRequest, writer.uint32(42).fork()).join();
        break;
      case "ping":
        Ping.encode(message.payload.ping, writer.uint32(50).fork()).join();
        break;
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): AppMessage {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseAppMessage();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.messageId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.sentAt = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.payload = { $case: "handshake", handshake: AppHandshake.decode(reader, reader.uint32()) };
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.payload = { $case: "command", command: DeviceCommand.decode(reader, reader.uint32()) };
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.payload = { $case: "readRequest", readRequest: PropertyReadRequest.decode(reader, reader.uint32()) };
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.payload = { $case: "ping", ping: Ping.decode(reader, reader.uint32()) };
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): AppMessage {
    return {
      messageId: isSet(object.messageId)
        ? globalThis.String(object.messageId)
        : isSet(object.message_id)
        ? globalThis.String(object.message_id)
        : "",
      sentAt: isSet(object.sentAt)
        ? fromJsonTimestamp(object.sentAt)
        : isSet(object.sent_at)
        ? fromJsonTimestamp(object.sent_at)
        : undefined,
      payload: isSet(object.handshake)
        ? { $case: "handshake", handshake: AppHandshake.fromJSON(object.handshake) }
        : isSet(object.command)
        ? { $case: "command", command: DeviceCommand.fromJSON(object.command) }
        : isSet(object.readRequest)
        ? { $case: "readRequest", readRequest: PropertyReadRequest.fromJSON(object.readRequest) }
        : isSet(object.read_request)
        ? { $case: "readRequest", readRequest: PropertyReadRequest.fromJSON(object.read_request) }
        : isSet(object.ping)
        ? { $case: "ping", ping: Ping.fromJSON(object.ping) }
        : undefined,
    };
  },

  toJSON(message: AppMessage): unknown {
    const obj: any = {};
    if (message.messageId !== undefined && message.messageId !== "") {
      obj.messageId = message.messageId;
    }
    if (message.sentAt !== undefined) {
      obj.sentAt = message.sentAt.toISOString();
    }
    if (message.payload?.$case === "handshake") {
      obj.handshake = AppHandshake.toJSON(message.payload.handshake);
    } else if (message.payload?.$case === "command") {
      obj.command = DeviceCommand.toJSON(message.payload.command);
    } else if (message.payload?.$case === "readRequest") {
      obj.readRequest = PropertyReadRequest.toJSON(message.payload.readRequest);
    } else if (message.payload?.$case === "ping") {
      obj.ping = Ping.toJSON(message.payload.ping);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<AppMessage>, I>>(base?: I): AppMessage {
    return AppMessage.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<AppMessage>, I>>(object: I): AppMessage {
    const message = createBaseAppMessage();
    message.messageId = object.messageId ?? "";
    message.sentAt = object.sentAt ?? undefined;
    switch (object.payload?.$case) {
      case "handshake": {
        if (object.payload?.handshake !== undefined && object.payload?.handshake !== null) {
          message.payload = { $case: "handshake", handshake: AppHandshake.fromPartial(object.payload.handshake) };
        }
        break;
      }
      case "command": {
        if (object.payload?.command !== undefined && object.payload?.command !== null) {
          message.payload = { $case: "command", command: DeviceCommand.fromPartial(object.payload.command) };
        }
        break;
      }
      case "readRequest": {
        if (object.payload?.readRequest !== undefined && object.payload?.readRequest !== null) {
          message.payload = {
            $case: "readRequest",
            readRequest: PropertyReadRequest.fromPartial(object.payload.readRequest),
          };
        }
        break;
      }
      case "ping": {
        if (object.payload?.ping !== undefined && object.payload?.ping !== null) {
          message.payload = { $case: "ping", ping: Ping.fromPartial(object.payload.ping) };
        }
        break;
      }
    }
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends { $case: string } ? { [K in keyof Omit<T, "$case">]?: DeepPartial<T[K]> } & { $case: T["$case"] }
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function toTimestamp(date: Date): Timestamp {
  const seconds = Math.trunc(date.getTime() / 1_000);
  const nanos = (date.getTime() % 1_000) * 1_000_000;
  return { seconds, nanos };
}

function fromTimestamp(t: Timestamp): Date {
  let millis = (t.seconds || 0) * 1_000;
  millis += (t.nanos || 0) / 1_000_000;
  return new globalThis.Date(millis);
}

function fromJsonTimestamp(o: any): Date {
  if (o instanceof globalThis.Date) {
    return o;
  } else if (typeof o === "string") {
    return new globalThis.Date(o);
  } else {
    return fromTimestamp(Timestamp.fromJSON(o));
  }
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
