// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.8
//   protoc               v5.27.3
// source: webrtc/device_state.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import { EffectivePermissions } from "../access/acl";
import { Device } from "../device/device";
import { PropertyUpdate } from "../device/property_update";
import { Timestamp } from "../google/protobuf/timestamp";

export const protobufPackage = "kusinta.iot.v1.webrtc";

/** Full device state sent to the Flutter app on initial WebRTC connection. */
export interface DeviceStateSnapshot {
  devices?: Device[] | undefined;
  permissions?: EffectivePermissions | undefined;
  snapshottedAt?: Date | undefined;
}

/** Single property change event streamed to the Flutter app in real time. */
export interface DevicePropertyEvent {
  update?: PropertyUpdate | undefined;
  gatewayProcessedAt?: Date | undefined;
}

function createBaseDeviceStateSnapshot(): DeviceStateSnapshot {
  return { devices: [], permissions: undefined, snapshottedAt: undefined };
}

export const DeviceStateSnapshot: MessageFns<DeviceStateSnapshot> = {
  encode(message: DeviceStateSnapshot, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.devices !== undefined && message.devices.length !== 0) {
      for (const v of message.devices) {
        Device.encode(v!, writer.uint32(10).fork()).join();
      }
    }
    if (message.permissions !== undefined) {
      EffectivePermissions.encode(message.permissions, writer.uint32(18).fork()).join();
    }
    if (message.snapshottedAt !== undefined) {
      Timestamp.encode(toTimestamp(message.snapshottedAt), writer.uint32(26).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): DeviceStateSnapshot {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseDeviceStateSnapshot();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          const el = Device.decode(reader, reader.uint32());
          if (el !== undefined) {
            message.devices!.push(el);
          }
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.permissions = EffectivePermissions.decode(reader, reader.uint32());
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.snapshottedAt = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): DeviceStateSnapshot {
    return {
      devices: globalThis.Array.isArray(object?.devices) ? object.devices.map((e: any) => Device.fromJSON(e)) : [],
      permissions: isSet(object.permissions) ? EffectivePermissions.fromJSON(object.permissions) : undefined,
      snapshottedAt: isSet(object.snapshottedAt)
        ? fromJsonTimestamp(object.snapshottedAt)
        : isSet(object.snapshotted_at)
        ? fromJsonTimestamp(object.snapshotted_at)
        : undefined,
    };
  },

  toJSON(message: DeviceStateSnapshot): unknown {
    const obj: any = {};
    if (message.devices?.length) {
      obj.devices = message.devices.map((e) => Device.toJSON(e));
    }
    if (message.permissions !== undefined) {
      obj.permissions = EffectivePermissions.toJSON(message.permissions);
    }
    if (message.snapshottedAt !== undefined) {
      obj.snapshottedAt = message.snapshottedAt.toISOString();
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<DeviceStateSnapshot>, I>>(base?: I): DeviceStateSnapshot {
    return DeviceStateSnapshot.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<DeviceStateSnapshot>, I>>(object: I): DeviceStateSnapshot {
    const message = createBaseDeviceStateSnapshot();
    message.devices = object.devices?.map((e) => Device.fromPartial(e)) || [];
    message.permissions = (object.permissions !== undefined && object.permissions !== null)
      ? EffectivePermissions.fromPartial(object.permissions)
      : undefined;
    message.snapshottedAt = object.snapshottedAt ?? undefined;
    return message;
  },
};

function createBaseDevicePropertyEvent(): DevicePropertyEvent {
  return { update: undefined, gatewayProcessedAt: undefined };
}

export const DevicePropertyEvent: MessageFns<DevicePropertyEvent> = {
  encode(message: DevicePropertyEvent, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.update !== undefined) {
      PropertyUpdate.encode(message.update, writer.uint32(10).fork()).join();
    }
    if (message.gatewayProcessedAt !== undefined) {
      Timestamp.encode(toTimestamp(message.gatewayProcessedAt), writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): DevicePropertyEvent {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseDevicePropertyEvent();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.update = PropertyUpdate.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.gatewayProcessedAt = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): DevicePropertyEvent {
    return {
      update: isSet(object.update) ? PropertyUpdate.fromJSON(object.update) : undefined,
      gatewayProcessedAt: isSet(object.gatewayProcessedAt)
        ? fromJsonTimestamp(object.gatewayProcessedAt)
        : isSet(object.gateway_processed_at)
        ? fromJsonTimestamp(object.gateway_processed_at)
        : undefined,
    };
  },

  toJSON(message: DevicePropertyEvent): unknown {
    const obj: any = {};
    if (message.update !== undefined) {
      obj.update = PropertyUpdate.toJSON(message.update);
    }
    if (message.gatewayProcessedAt !== undefined) {
      obj.gatewayProcessedAt = message.gatewayProcessedAt.toISOString();
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<DevicePropertyEvent>, I>>(base?: I): DevicePropertyEvent {
    return DevicePropertyEvent.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<DevicePropertyEvent>, I>>(object: I): DevicePropertyEvent {
    const message = createBaseDevicePropertyEvent();
    message.update = (object.update !== undefined && object.update !== null)
      ? PropertyUpdate.fromPartial(object.update)
      : undefined;
    message.gatewayProcessedAt = object.gatewayProcessedAt ?? undefined;
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends { $case: string } ? { [K in keyof Omit<T, "$case">]?: DeepPartial<T[K]> } & { $case: T["$case"] }
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function toTimestamp(date: Date): Timestamp {
  const seconds = Math.trunc(date.getTime() / 1_000);
  const nanos = (date.getTime() % 1_000) * 1_000_000;
  return { seconds, nanos };
}

function fromTimestamp(t: Timestamp): Date {
  let millis = (t.seconds || 0) * 1_000;
  millis += (t.nanos || 0) / 1_000_000;
  return new globalThis.Date(millis);
}

function fromJsonTimestamp(o: any): Date {
  if (o instanceof globalThis.Date) {
    return o;
  } else if (typeof o === "string") {
    return new globalThis.Date(o);
  } else {
    return fromTimestamp(Timestamp.fromJSON(o));
  }
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
