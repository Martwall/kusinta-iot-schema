// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.8
//   protoc               v5.27.3
// source: common/types.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";

export const protobufPackage = "kusinta.iot.v1.common";

export enum SpaceType {
  SPACE_TYPE_UNSPECIFIED = "SPACE_TYPE_UNSPECIFIED",
  BUILDING = "BUILDING",
  FLOOR = "FLOOR",
  APARTMENT = "APARTMENT",
  ROOM = "ROOM",
  COMMON_AREA = "COMMON_AREA",
  UNRECOGNIZED = "UNRECOGNIZED",
}

export function spaceTypeFromJSON(object: any): SpaceType {
  switch (object) {
    case 0:
    case "SPACE_TYPE_UNSPECIFIED":
      return SpaceType.SPACE_TYPE_UNSPECIFIED;
    case 1:
    case "BUILDING":
      return SpaceType.BUILDING;
    case 2:
    case "FLOOR":
      return SpaceType.FLOOR;
    case 3:
    case "APARTMENT":
      return SpaceType.APARTMENT;
    case 4:
    case "ROOM":
      return SpaceType.ROOM;
    case 5:
    case "COMMON_AREA":
      return SpaceType.COMMON_AREA;
    case -1:
    case "UNRECOGNIZED":
    default:
      return SpaceType.UNRECOGNIZED;
  }
}

export function spaceTypeToJSON(object: SpaceType): string {
  switch (object) {
    case SpaceType.SPACE_TYPE_UNSPECIFIED:
      return "SPACE_TYPE_UNSPECIFIED";
    case SpaceType.BUILDING:
      return "BUILDING";
    case SpaceType.FLOOR:
      return "FLOOR";
    case SpaceType.APARTMENT:
      return "APARTMENT";
    case SpaceType.ROOM:
      return "ROOM";
    case SpaceType.COMMON_AREA:
      return "COMMON_AREA";
    case SpaceType.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

export function spaceTypeToNumber(object: SpaceType): number {
  switch (object) {
    case SpaceType.SPACE_TYPE_UNSPECIFIED:
      return 0;
    case SpaceType.BUILDING:
      return 1;
    case SpaceType.FLOOR:
      return 2;
    case SpaceType.APARTMENT:
      return 3;
    case SpaceType.ROOM:
      return 4;
    case SpaceType.COMMON_AREA:
      return 5;
    case SpaceType.UNRECOGNIZED:
    default:
      return -1;
  }
}

export enum DeviceOwnershipType {
  DEVICE_OWNERSHIP_TYPE_UNSPECIFIED = "DEVICE_OWNERSHIP_TYPE_UNSPECIFIED",
  COMPANY = "COMPANY",
  RESIDENT = "RESIDENT",
  UNRECOGNIZED = "UNRECOGNIZED",
}

export function deviceOwnershipTypeFromJSON(object: any): DeviceOwnershipType {
  switch (object) {
    case 0:
    case "DEVICE_OWNERSHIP_TYPE_UNSPECIFIED":
      return DeviceOwnershipType.DEVICE_OWNERSHIP_TYPE_UNSPECIFIED;
    case 1:
    case "COMPANY":
      return DeviceOwnershipType.COMPANY;
    case 2:
    case "RESIDENT":
      return DeviceOwnershipType.RESIDENT;
    case -1:
    case "UNRECOGNIZED":
    default:
      return DeviceOwnershipType.UNRECOGNIZED;
  }
}

export function deviceOwnershipTypeToJSON(object: DeviceOwnershipType): string {
  switch (object) {
    case DeviceOwnershipType.DEVICE_OWNERSHIP_TYPE_UNSPECIFIED:
      return "DEVICE_OWNERSHIP_TYPE_UNSPECIFIED";
    case DeviceOwnershipType.COMPANY:
      return "COMPANY";
    case DeviceOwnershipType.RESIDENT:
      return "RESIDENT";
    case DeviceOwnershipType.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

export function deviceOwnershipTypeToNumber(object: DeviceOwnershipType): number {
  switch (object) {
    case DeviceOwnershipType.DEVICE_OWNERSHIP_TYPE_UNSPECIFIED:
      return 0;
    case DeviceOwnershipType.COMPANY:
      return 1;
    case DeviceOwnershipType.RESIDENT:
      return 2;
    case DeviceOwnershipType.UNRECOGNIZED:
    default:
      return -1;
  }
}

export enum DeviceLifecycleState {
  DEVICE_LIFECYCLE_STATE_UNSPECIFIED = "DEVICE_LIFECYCLE_STATE_UNSPECIFIED",
  PENDING_CLAIM = "PENDING_CLAIM",
  OWNED = "OWNED",
  PENDING_TRANSFER = "PENDING_TRANSFER",
  BUILDING_POOL = "BUILDING_POOL",
  UNRECOGNIZED = "UNRECOGNIZED",
}

export function deviceLifecycleStateFromJSON(object: any): DeviceLifecycleState {
  switch (object) {
    case 0:
    case "DEVICE_LIFECYCLE_STATE_UNSPECIFIED":
      return DeviceLifecycleState.DEVICE_LIFECYCLE_STATE_UNSPECIFIED;
    case 1:
    case "PENDING_CLAIM":
      return DeviceLifecycleState.PENDING_CLAIM;
    case 2:
    case "OWNED":
      return DeviceLifecycleState.OWNED;
    case 3:
    case "PENDING_TRANSFER":
      return DeviceLifecycleState.PENDING_TRANSFER;
    case 4:
    case "BUILDING_POOL":
      return DeviceLifecycleState.BUILDING_POOL;
    case -1:
    case "UNRECOGNIZED":
    default:
      return DeviceLifecycleState.UNRECOGNIZED;
  }
}

export function deviceLifecycleStateToJSON(object: DeviceLifecycleState): string {
  switch (object) {
    case DeviceLifecycleState.DEVICE_LIFECYCLE_STATE_UNSPECIFIED:
      return "DEVICE_LIFECYCLE_STATE_UNSPECIFIED";
    case DeviceLifecycleState.PENDING_CLAIM:
      return "PENDING_CLAIM";
    case DeviceLifecycleState.OWNED:
      return "OWNED";
    case DeviceLifecycleState.PENDING_TRANSFER:
      return "PENDING_TRANSFER";
    case DeviceLifecycleState.BUILDING_POOL:
      return "BUILDING_POOL";
    case DeviceLifecycleState.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

export function deviceLifecycleStateToNumber(object: DeviceLifecycleState): number {
  switch (object) {
    case DeviceLifecycleState.DEVICE_LIFECYCLE_STATE_UNSPECIFIED:
      return 0;
    case DeviceLifecycleState.PENDING_CLAIM:
      return 1;
    case DeviceLifecycleState.OWNED:
      return 2;
    case DeviceLifecycleState.PENDING_TRANSFER:
      return 3;
    case DeviceLifecycleState.BUILDING_POOL:
      return 4;
    case DeviceLifecycleState.UNRECOGNIZED:
    default:
      return -1;
  }
}

export enum ConnectorTransport {
  CONNECTOR_TRANSPORT_UNSPECIFIED = "CONNECTOR_TRANSPORT_UNSPECIFIED",
  UNIX_SOCKET = "UNIX_SOCKET",
  MTLS_TCP = "MTLS_TCP",
  UNRECOGNIZED = "UNRECOGNIZED",
}

export function connectorTransportFromJSON(object: any): ConnectorTransport {
  switch (object) {
    case 0:
    case "CONNECTOR_TRANSPORT_UNSPECIFIED":
      return ConnectorTransport.CONNECTOR_TRANSPORT_UNSPECIFIED;
    case 1:
    case "UNIX_SOCKET":
      return ConnectorTransport.UNIX_SOCKET;
    case 2:
    case "MTLS_TCP":
      return ConnectorTransport.MTLS_TCP;
    case -1:
    case "UNRECOGNIZED":
    default:
      return ConnectorTransport.UNRECOGNIZED;
  }
}

export function connectorTransportToJSON(object: ConnectorTransport): string {
  switch (object) {
    case ConnectorTransport.CONNECTOR_TRANSPORT_UNSPECIFIED:
      return "CONNECTOR_TRANSPORT_UNSPECIFIED";
    case ConnectorTransport.UNIX_SOCKET:
      return "UNIX_SOCKET";
    case ConnectorTransport.MTLS_TCP:
      return "MTLS_TCP";
    case ConnectorTransport.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

export function connectorTransportToNumber(object: ConnectorTransport): number {
  switch (object) {
    case ConnectorTransport.CONNECTOR_TRANSPORT_UNSPECIFIED:
      return 0;
    case ConnectorTransport.UNIX_SOCKET:
      return 1;
    case ConnectorTransport.MTLS_TCP:
      return 2;
    case ConnectorTransport.UNRECOGNIZED:
    default:
      return -1;
  }
}

export interface ErrorDetail {
  code?: string | undefined;
  message?: string | undefined;
  metadata?: { [key: string]: string } | undefined;
}

export interface ErrorDetail_MetadataEntry {
  key: string;
  value: string;
}

function createBaseErrorDetail(): ErrorDetail {
  return { code: "", message: "", metadata: {} };
}

export const ErrorDetail: MessageFns<ErrorDetail> = {
  encode(message: ErrorDetail, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.code !== undefined && message.code !== "") {
      writer.uint32(10).string(message.code);
    }
    if (message.message !== undefined && message.message !== "") {
      writer.uint32(18).string(message.message);
    }
    globalThis.Object.entries(message.metadata || {}).forEach(([key, value]: [string, string]) => {
      ErrorDetail_MetadataEntry.encode({ key: key as any, value }, writer.uint32(26).fork()).join();
    });
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ErrorDetail {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseErrorDetail();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.code = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.message = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          const entry3 = ErrorDetail_MetadataEntry.decode(reader, reader.uint32());
          if (entry3.value !== undefined) {
            message.metadata![entry3.key] = entry3.value;
          }
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ErrorDetail {
    return {
      code: isSet(object.code) ? globalThis.String(object.code) : "",
      message: isSet(object.message) ? globalThis.String(object.message) : "",
      metadata: isObject(object.metadata)
        ? (globalThis.Object.entries(object.metadata) as [string, any][]).reduce(
          (acc: { [key: string]: string }, [key, value]: [string, any]) => {
            acc[key] = globalThis.String(value);
            return acc;
          },
          {},
        )
        : {},
    };
  },

  toJSON(message: ErrorDetail): unknown {
    const obj: any = {};
    if (message.code !== undefined && message.code !== "") {
      obj.code = message.code;
    }
    if (message.message !== undefined && message.message !== "") {
      obj.message = message.message;
    }
    if (message.metadata) {
      const entries = globalThis.Object.entries(message.metadata) as [string, string][];
      if (entries.length > 0) {
        obj.metadata = {};
        entries.forEach(([k, v]) => {
          obj.metadata[k] = v;
        });
      }
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ErrorDetail>, I>>(base?: I): ErrorDetail {
    return ErrorDetail.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ErrorDetail>, I>>(object: I): ErrorDetail {
    const message = createBaseErrorDetail();
    message.code = object.code ?? "";
    message.message = object.message ?? "";
    message.metadata = (globalThis.Object.entries(object.metadata ?? {}) as [string, string][]).reduce(
      (acc: { [key: string]: string }, [key, value]: [string, string]) => {
        if (value !== undefined) {
          acc[key] = globalThis.String(value);
        }
        return acc;
      },
      {},
    );
    return message;
  },
};

function createBaseErrorDetail_MetadataEntry(): ErrorDetail_MetadataEntry {
  return { key: "", value: "" };
}

export const ErrorDetail_MetadataEntry: MessageFns<ErrorDetail_MetadataEntry> = {
  encode(message: ErrorDetail_MetadataEntry, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.key !== "") {
      writer.uint32(10).string(message.key);
    }
    if (message.value !== "") {
      writer.uint32(18).string(message.value);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ErrorDetail_MetadataEntry {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseErrorDetail_MetadataEntry();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.key = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.value = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ErrorDetail_MetadataEntry {
    return {
      key: isSet(object.key) ? globalThis.String(object.key) : "",
      value: isSet(object.value) ? globalThis.String(object.value) : "",
    };
  },

  toJSON(message: ErrorDetail_MetadataEntry): unknown {
    const obj: any = {};
    if (message.key !== "") {
      obj.key = message.key;
    }
    if (message.value !== "") {
      obj.value = message.value;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ErrorDetail_MetadataEntry>, I>>(base?: I): ErrorDetail_MetadataEntry {
    return ErrorDetail_MetadataEntry.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ErrorDetail_MetadataEntry>, I>>(object: I): ErrorDetail_MetadataEntry {
    const message = createBaseErrorDetail_MetadataEntry();
    message.key = object.key ?? "";
    message.value = object.value ?? "";
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends { $case: string } ? { [K in keyof Omit<T, "$case">]?: DeepPartial<T[K]> } & { $case: T["$case"] }
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function isObject(value: any): boolean {
  return typeof value === "object" && value !== null;
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
