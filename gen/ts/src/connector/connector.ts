// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.8
//   protoc               v5.27.3
// source: connector/connector.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import {
  ConnectorTransport,
  connectorTransportFromJSON,
  connectorTransportToJSON,
  connectorTransportToNumber,
} from "../common/types";
import { DeviceDescriptor } from "../device/descriptor";
import { PropertyUpdateBatch } from "../device/property_update";
import { Timestamp } from "../google/protobuf/timestamp";
import { ConnectorId, DeviceId, GatewayId } from "../identity/identity";

export const protobufPackage = "kusinta.iot.v1.connector";

export interface ConnectorInfo {
  connectorId?: ConnectorId | undefined;
  displayName?: string | undefined;
  version?: string | undefined;
  transport?:
    | ConnectorTransport
    | undefined;
  /** unix socket path or host:port */
  endpoint?: string | undefined;
  supportedDeviceTypeIds?: number[] | undefined;
}

export interface ConnectorHandshake {
  info?: ConnectorInfo | undefined;
  knownDevices?: DeviceDescriptor[] | undefined;
}

export interface HandshakeAck {
  accepted?: boolean | undefined;
  reason?: string | undefined;
  gatewayId?: GatewayId | undefined;
}

export interface DeviceAnnouncement {
  descriptor?: DeviceDescriptor | undefined;
}

export interface DeviceRemoval {
  deviceId?: DeviceId | undefined;
  reason?: string | undefined;
}

export interface HeartBeat {
}

export interface SubscribeDevice {
  deviceId?: DeviceId | undefined;
}

export interface UnsubscribeDevice {
  deviceId?: DeviceId | undefined;
}

export interface GatewayError {
  code?: string | undefined;
  message?:
    | string
    | undefined;
  /** non-empty if the error is in response to a command */
  commandId?: string | undefined;
}

export interface CommandResult {
  commandId?: string | undefined;
  success?: boolean | undefined;
  error?: GatewayError | undefined;
  completedAt?: Date | undefined;
}

export interface ConnectorToGatewayMessage {
  messageId?: string | undefined;
  sentAt?: Date | undefined;
  payload?:
    | { $case: "handshake"; handshake: ConnectorHandshake }
    | { $case: "propertyUpdate"; propertyUpdate: PropertyUpdateBatch }
    | { $case: "deviceAnnounced"; deviceAnnounced: DeviceAnnouncement }
    | { $case: "deviceRemoved"; deviceRemoved: DeviceRemoval }
    | { $case: "commandResult"; commandResult: CommandResult }
    | { $case: "heartbeat"; heartbeat: HeartBeat }
    | undefined;
}

export interface GatewayToConnectorMessage {
  messageId?: string | undefined;
  sentAt?: Date | undefined;
  payload?:
    | { $case: "handshakeAck"; handshakeAck: HandshakeAck }
    | { $case: "subscribe"; subscribe: SubscribeDevice }
    | { $case: "unsubscribe"; unsubscribe: UnsubscribeDevice }
    | { $case: "error"; error: GatewayError }
    | undefined;
}

function createBaseConnectorInfo(): ConnectorInfo {
  return {
    connectorId: undefined,
    displayName: "",
    version: "",
    transport: ConnectorTransport.CONNECTOR_TRANSPORT_UNSPECIFIED,
    endpoint: "",
    supportedDeviceTypeIds: [],
  };
}

export const ConnectorInfo: MessageFns<ConnectorInfo> = {
  encode(message: ConnectorInfo, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.connectorId !== undefined) {
      ConnectorId.encode(message.connectorId, writer.uint32(10).fork()).join();
    }
    if (message.displayName !== undefined && message.displayName !== "") {
      writer.uint32(18).string(message.displayName);
    }
    if (message.version !== undefined && message.version !== "") {
      writer.uint32(26).string(message.version);
    }
    if (message.transport !== undefined && message.transport !== ConnectorTransport.CONNECTOR_TRANSPORT_UNSPECIFIED) {
      writer.uint32(32).int32(connectorTransportToNumber(message.transport));
    }
    if (message.endpoint !== undefined && message.endpoint !== "") {
      writer.uint32(42).string(message.endpoint);
    }
    if (message.supportedDeviceTypeIds !== undefined && message.supportedDeviceTypeIds.length !== 0) {
      writer.uint32(50).fork();
      for (const v of message.supportedDeviceTypeIds) {
        writer.uint32(v);
      }
      writer.join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ConnectorInfo {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseConnectorInfo();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.connectorId = ConnectorId.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.displayName = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.version = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.transport = connectorTransportFromJSON(reader.int32());
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.endpoint = reader.string();
          continue;
        }
        case 6: {
          if (tag === 48) {
            message.supportedDeviceTypeIds!.push(reader.uint32());

            continue;
          }

          if (tag === 50) {
            const end2 = reader.uint32() + reader.pos;
            while (reader.pos < end2) {
              message.supportedDeviceTypeIds!.push(reader.uint32());
            }

            continue;
          }

          break;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ConnectorInfo {
    return {
      connectorId: isSet(object.connectorId)
        ? ConnectorId.fromJSON(object.connectorId)
        : isSet(object.connector_id)
        ? ConnectorId.fromJSON(object.connector_id)
        : undefined,
      displayName: isSet(object.displayName)
        ? globalThis.String(object.displayName)
        : isSet(object.display_name)
        ? globalThis.String(object.display_name)
        : "",
      version: isSet(object.version) ? globalThis.String(object.version) : "",
      transport: isSet(object.transport)
        ? connectorTransportFromJSON(object.transport)
        : ConnectorTransport.CONNECTOR_TRANSPORT_UNSPECIFIED,
      endpoint: isSet(object.endpoint) ? globalThis.String(object.endpoint) : "",
      supportedDeviceTypeIds: globalThis.Array.isArray(object?.supportedDeviceTypeIds)
        ? object.supportedDeviceTypeIds.map((e: any) => globalThis.Number(e))
        : globalThis.Array.isArray(object?.supported_device_type_ids)
        ? object.supported_device_type_ids.map((e: any) => globalThis.Number(e))
        : [],
    };
  },

  toJSON(message: ConnectorInfo): unknown {
    const obj: any = {};
    if (message.connectorId !== undefined) {
      obj.connectorId = ConnectorId.toJSON(message.connectorId);
    }
    if (message.displayName !== undefined && message.displayName !== "") {
      obj.displayName = message.displayName;
    }
    if (message.version !== undefined && message.version !== "") {
      obj.version = message.version;
    }
    if (message.transport !== undefined && message.transport !== ConnectorTransport.CONNECTOR_TRANSPORT_UNSPECIFIED) {
      obj.transport = connectorTransportToJSON(message.transport);
    }
    if (message.endpoint !== undefined && message.endpoint !== "") {
      obj.endpoint = message.endpoint;
    }
    if (message.supportedDeviceTypeIds?.length) {
      obj.supportedDeviceTypeIds = message.supportedDeviceTypeIds.map((e) => Math.round(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ConnectorInfo>, I>>(base?: I): ConnectorInfo {
    return ConnectorInfo.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ConnectorInfo>, I>>(object: I): ConnectorInfo {
    const message = createBaseConnectorInfo();
    message.connectorId = (object.connectorId !== undefined && object.connectorId !== null)
      ? ConnectorId.fromPartial(object.connectorId)
      : undefined;
    message.displayName = object.displayName ?? "";
    message.version = object.version ?? "";
    message.transport = object.transport ?? ConnectorTransport.CONNECTOR_TRANSPORT_UNSPECIFIED;
    message.endpoint = object.endpoint ?? "";
    message.supportedDeviceTypeIds = object.supportedDeviceTypeIds?.map((e) => e) || [];
    return message;
  },
};

function createBaseConnectorHandshake(): ConnectorHandshake {
  return { info: undefined, knownDevices: [] };
}

export const ConnectorHandshake: MessageFns<ConnectorHandshake> = {
  encode(message: ConnectorHandshake, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.info !== undefined) {
      ConnectorInfo.encode(message.info, writer.uint32(10).fork()).join();
    }
    if (message.knownDevices !== undefined && message.knownDevices.length !== 0) {
      for (const v of message.knownDevices) {
        DeviceDescriptor.encode(v!, writer.uint32(18).fork()).join();
      }
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ConnectorHandshake {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseConnectorHandshake();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.info = ConnectorInfo.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          const el = DeviceDescriptor.decode(reader, reader.uint32());
          if (el !== undefined) {
            message.knownDevices!.push(el);
          }
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ConnectorHandshake {
    return {
      info: isSet(object.info) ? ConnectorInfo.fromJSON(object.info) : undefined,
      knownDevices: globalThis.Array.isArray(object?.knownDevices)
        ? object.knownDevices.map((e: any) => DeviceDescriptor.fromJSON(e))
        : globalThis.Array.isArray(object?.known_devices)
        ? object.known_devices.map((e: any) => DeviceDescriptor.fromJSON(e))
        : [],
    };
  },

  toJSON(message: ConnectorHandshake): unknown {
    const obj: any = {};
    if (message.info !== undefined) {
      obj.info = ConnectorInfo.toJSON(message.info);
    }
    if (message.knownDevices?.length) {
      obj.knownDevices = message.knownDevices.map((e) => DeviceDescriptor.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ConnectorHandshake>, I>>(base?: I): ConnectorHandshake {
    return ConnectorHandshake.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ConnectorHandshake>, I>>(object: I): ConnectorHandshake {
    const message = createBaseConnectorHandshake();
    message.info = (object.info !== undefined && object.info !== null)
      ? ConnectorInfo.fromPartial(object.info)
      : undefined;
    message.knownDevices = object.knownDevices?.map((e) => DeviceDescriptor.fromPartial(e)) || [];
    return message;
  },
};

function createBaseHandshakeAck(): HandshakeAck {
  return { accepted: false, reason: "", gatewayId: undefined };
}

export const HandshakeAck: MessageFns<HandshakeAck> = {
  encode(message: HandshakeAck, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.accepted !== undefined && message.accepted !== false) {
      writer.uint32(8).bool(message.accepted);
    }
    if (message.reason !== undefined && message.reason !== "") {
      writer.uint32(18).string(message.reason);
    }
    if (message.gatewayId !== undefined) {
      GatewayId.encode(message.gatewayId, writer.uint32(26).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): HandshakeAck {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseHandshakeAck();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.accepted = reader.bool();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.reason = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.gatewayId = GatewayId.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): HandshakeAck {
    return {
      accepted: isSet(object.accepted) ? globalThis.Boolean(object.accepted) : false,
      reason: isSet(object.reason) ? globalThis.String(object.reason) : "",
      gatewayId: isSet(object.gatewayId)
        ? GatewayId.fromJSON(object.gatewayId)
        : isSet(object.gateway_id)
        ? GatewayId.fromJSON(object.gateway_id)
        : undefined,
    };
  },

  toJSON(message: HandshakeAck): unknown {
    const obj: any = {};
    if (message.accepted !== undefined && message.accepted !== false) {
      obj.accepted = message.accepted;
    }
    if (message.reason !== undefined && message.reason !== "") {
      obj.reason = message.reason;
    }
    if (message.gatewayId !== undefined) {
      obj.gatewayId = GatewayId.toJSON(message.gatewayId);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<HandshakeAck>, I>>(base?: I): HandshakeAck {
    return HandshakeAck.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<HandshakeAck>, I>>(object: I): HandshakeAck {
    const message = createBaseHandshakeAck();
    message.accepted = object.accepted ?? false;
    message.reason = object.reason ?? "";
    message.gatewayId = (object.gatewayId !== undefined && object.gatewayId !== null)
      ? GatewayId.fromPartial(object.gatewayId)
      : undefined;
    return message;
  },
};

function createBaseDeviceAnnouncement(): DeviceAnnouncement {
  return { descriptor: undefined };
}

export const DeviceAnnouncement: MessageFns<DeviceAnnouncement> = {
  encode(message: DeviceAnnouncement, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.descriptor !== undefined) {
      DeviceDescriptor.encode(message.descriptor, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): DeviceAnnouncement {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseDeviceAnnouncement();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.descriptor = DeviceDescriptor.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): DeviceAnnouncement {
    return { descriptor: isSet(object.descriptor) ? DeviceDescriptor.fromJSON(object.descriptor) : undefined };
  },

  toJSON(message: DeviceAnnouncement): unknown {
    const obj: any = {};
    if (message.descriptor !== undefined) {
      obj.descriptor = DeviceDescriptor.toJSON(message.descriptor);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<DeviceAnnouncement>, I>>(base?: I): DeviceAnnouncement {
    return DeviceAnnouncement.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<DeviceAnnouncement>, I>>(object: I): DeviceAnnouncement {
    const message = createBaseDeviceAnnouncement();
    message.descriptor = (object.descriptor !== undefined && object.descriptor !== null)
      ? DeviceDescriptor.fromPartial(object.descriptor)
      : undefined;
    return message;
  },
};

function createBaseDeviceRemoval(): DeviceRemoval {
  return { deviceId: undefined, reason: "" };
}

export const DeviceRemoval: MessageFns<DeviceRemoval> = {
  encode(message: DeviceRemoval, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.deviceId !== undefined) {
      DeviceId.encode(message.deviceId, writer.uint32(10).fork()).join();
    }
    if (message.reason !== undefined && message.reason !== "") {
      writer.uint32(18).string(message.reason);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): DeviceRemoval {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseDeviceRemoval();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.deviceId = DeviceId.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.reason = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): DeviceRemoval {
    return {
      deviceId: isSet(object.deviceId)
        ? DeviceId.fromJSON(object.deviceId)
        : isSet(object.device_id)
        ? DeviceId.fromJSON(object.device_id)
        : undefined,
      reason: isSet(object.reason) ? globalThis.String(object.reason) : "",
    };
  },

  toJSON(message: DeviceRemoval): unknown {
    const obj: any = {};
    if (message.deviceId !== undefined) {
      obj.deviceId = DeviceId.toJSON(message.deviceId);
    }
    if (message.reason !== undefined && message.reason !== "") {
      obj.reason = message.reason;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<DeviceRemoval>, I>>(base?: I): DeviceRemoval {
    return DeviceRemoval.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<DeviceRemoval>, I>>(object: I): DeviceRemoval {
    const message = createBaseDeviceRemoval();
    message.deviceId = (object.deviceId !== undefined && object.deviceId !== null)
      ? DeviceId.fromPartial(object.deviceId)
      : undefined;
    message.reason = object.reason ?? "";
    return message;
  },
};

function createBaseHeartBeat(): HeartBeat {
  return {};
}

export const HeartBeat: MessageFns<HeartBeat> = {
  encode(_: HeartBeat, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): HeartBeat {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseHeartBeat();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(_: any): HeartBeat {
    return {};
  },

  toJSON(_: HeartBeat): unknown {
    const obj: any = {};
    return obj;
  },

  create<I extends Exact<DeepPartial<HeartBeat>, I>>(base?: I): HeartBeat {
    return HeartBeat.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<HeartBeat>, I>>(_: I): HeartBeat {
    const message = createBaseHeartBeat();
    return message;
  },
};

function createBaseSubscribeDevice(): SubscribeDevice {
  return { deviceId: undefined };
}

export const SubscribeDevice: MessageFns<SubscribeDevice> = {
  encode(message: SubscribeDevice, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.deviceId !== undefined) {
      DeviceId.encode(message.deviceId, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SubscribeDevice {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSubscribeDevice();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.deviceId = DeviceId.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SubscribeDevice {
    return {
      deviceId: isSet(object.deviceId)
        ? DeviceId.fromJSON(object.deviceId)
        : isSet(object.device_id)
        ? DeviceId.fromJSON(object.device_id)
        : undefined,
    };
  },

  toJSON(message: SubscribeDevice): unknown {
    const obj: any = {};
    if (message.deviceId !== undefined) {
      obj.deviceId = DeviceId.toJSON(message.deviceId);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<SubscribeDevice>, I>>(base?: I): SubscribeDevice {
    return SubscribeDevice.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<SubscribeDevice>, I>>(object: I): SubscribeDevice {
    const message = createBaseSubscribeDevice();
    message.deviceId = (object.deviceId !== undefined && object.deviceId !== null)
      ? DeviceId.fromPartial(object.deviceId)
      : undefined;
    return message;
  },
};

function createBaseUnsubscribeDevice(): UnsubscribeDevice {
  return { deviceId: undefined };
}

export const UnsubscribeDevice: MessageFns<UnsubscribeDevice> = {
  encode(message: UnsubscribeDevice, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.deviceId !== undefined) {
      DeviceId.encode(message.deviceId, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): UnsubscribeDevice {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseUnsubscribeDevice();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.deviceId = DeviceId.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): UnsubscribeDevice {
    return {
      deviceId: isSet(object.deviceId)
        ? DeviceId.fromJSON(object.deviceId)
        : isSet(object.device_id)
        ? DeviceId.fromJSON(object.device_id)
        : undefined,
    };
  },

  toJSON(message: UnsubscribeDevice): unknown {
    const obj: any = {};
    if (message.deviceId !== undefined) {
      obj.deviceId = DeviceId.toJSON(message.deviceId);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<UnsubscribeDevice>, I>>(base?: I): UnsubscribeDevice {
    return UnsubscribeDevice.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<UnsubscribeDevice>, I>>(object: I): UnsubscribeDevice {
    const message = createBaseUnsubscribeDevice();
    message.deviceId = (object.deviceId !== undefined && object.deviceId !== null)
      ? DeviceId.fromPartial(object.deviceId)
      : undefined;
    return message;
  },
};

function createBaseGatewayError(): GatewayError {
  return { code: "", message: "", commandId: "" };
}

export const GatewayError: MessageFns<GatewayError> = {
  encode(message: GatewayError, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.code !== undefined && message.code !== "") {
      writer.uint32(10).string(message.code);
    }
    if (message.message !== undefined && message.message !== "") {
      writer.uint32(18).string(message.message);
    }
    if (message.commandId !== undefined && message.commandId !== "") {
      writer.uint32(26).string(message.commandId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GatewayError {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGatewayError();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.code = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.message = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.commandId = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GatewayError {
    return {
      code: isSet(object.code) ? globalThis.String(object.code) : "",
      message: isSet(object.message) ? globalThis.String(object.message) : "",
      commandId: isSet(object.commandId)
        ? globalThis.String(object.commandId)
        : isSet(object.command_id)
        ? globalThis.String(object.command_id)
        : "",
    };
  },

  toJSON(message: GatewayError): unknown {
    const obj: any = {};
    if (message.code !== undefined && message.code !== "") {
      obj.code = message.code;
    }
    if (message.message !== undefined && message.message !== "") {
      obj.message = message.message;
    }
    if (message.commandId !== undefined && message.commandId !== "") {
      obj.commandId = message.commandId;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GatewayError>, I>>(base?: I): GatewayError {
    return GatewayError.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GatewayError>, I>>(object: I): GatewayError {
    const message = createBaseGatewayError();
    message.code = object.code ?? "";
    message.message = object.message ?? "";
    message.commandId = object.commandId ?? "";
    return message;
  },
};

function createBaseCommandResult(): CommandResult {
  return { commandId: "", success: false, error: undefined, completedAt: undefined };
}

export const CommandResult: MessageFns<CommandResult> = {
  encode(message: CommandResult, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.commandId !== undefined && message.commandId !== "") {
      writer.uint32(10).string(message.commandId);
    }
    if (message.success !== undefined && message.success !== false) {
      writer.uint32(16).bool(message.success);
    }
    if (message.error !== undefined) {
      GatewayError.encode(message.error, writer.uint32(26).fork()).join();
    }
    if (message.completedAt !== undefined) {
      Timestamp.encode(toTimestamp(message.completedAt), writer.uint32(34).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): CommandResult {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseCommandResult();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.commandId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.success = reader.bool();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.error = GatewayError.decode(reader, reader.uint32());
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.completedAt = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): CommandResult {
    return {
      commandId: isSet(object.commandId)
        ? globalThis.String(object.commandId)
        : isSet(object.command_id)
        ? globalThis.String(object.command_id)
        : "",
      success: isSet(object.success) ? globalThis.Boolean(object.success) : false,
      error: isSet(object.error) ? GatewayError.fromJSON(object.error) : undefined,
      completedAt: isSet(object.completedAt)
        ? fromJsonTimestamp(object.completedAt)
        : isSet(object.completed_at)
        ? fromJsonTimestamp(object.completed_at)
        : undefined,
    };
  },

  toJSON(message: CommandResult): unknown {
    const obj: any = {};
    if (message.commandId !== undefined && message.commandId !== "") {
      obj.commandId = message.commandId;
    }
    if (message.success !== undefined && message.success !== false) {
      obj.success = message.success;
    }
    if (message.error !== undefined) {
      obj.error = GatewayError.toJSON(message.error);
    }
    if (message.completedAt !== undefined) {
      obj.completedAt = message.completedAt.toISOString();
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<CommandResult>, I>>(base?: I): CommandResult {
    return CommandResult.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<CommandResult>, I>>(object: I): CommandResult {
    const message = createBaseCommandResult();
    message.commandId = object.commandId ?? "";
    message.success = object.success ?? false;
    message.error = (object.error !== undefined && object.error !== null)
      ? GatewayError.fromPartial(object.error)
      : undefined;
    message.completedAt = object.completedAt ?? undefined;
    return message;
  },
};

function createBaseConnectorToGatewayMessage(): ConnectorToGatewayMessage {
  return { messageId: "", sentAt: undefined, payload: undefined };
}

export const ConnectorToGatewayMessage: MessageFns<ConnectorToGatewayMessage> = {
  encode(message: ConnectorToGatewayMessage, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.messageId !== undefined && message.messageId !== "") {
      writer.uint32(10).string(message.messageId);
    }
    if (message.sentAt !== undefined) {
      Timestamp.encode(toTimestamp(message.sentAt), writer.uint32(18).fork()).join();
    }
    switch (message.payload?.$case) {
      case "handshake":
        ConnectorHandshake.encode(message.payload.handshake, writer.uint32(26).fork()).join();
        break;
      case "propertyUpdate":
        PropertyUpdateBatch.encode(message.payload.propertyUpdate, writer.uint32(34).fork()).join();
        break;
      case "deviceAnnounced":
        DeviceAnnouncement.encode(message.payload.deviceAnnounced, writer.uint32(42).fork()).join();
        break;
      case "deviceRemoved":
        DeviceRemoval.encode(message.payload.deviceRemoved, writer.uint32(50).fork()).join();
        break;
      case "commandResult":
        CommandResult.encode(message.payload.commandResult, writer.uint32(58).fork()).join();
        break;
      case "heartbeat":
        HeartBeat.encode(message.payload.heartbeat, writer.uint32(66).fork()).join();
        break;
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ConnectorToGatewayMessage {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseConnectorToGatewayMessage();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.messageId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.sentAt = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.payload = { $case: "handshake", handshake: ConnectorHandshake.decode(reader, reader.uint32()) };
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.payload = {
            $case: "propertyUpdate",
            propertyUpdate: PropertyUpdateBatch.decode(reader, reader.uint32()),
          };
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.payload = {
            $case: "deviceAnnounced",
            deviceAnnounced: DeviceAnnouncement.decode(reader, reader.uint32()),
          };
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.payload = { $case: "deviceRemoved", deviceRemoved: DeviceRemoval.decode(reader, reader.uint32()) };
          continue;
        }
        case 7: {
          if (tag !== 58) {
            break;
          }

          message.payload = { $case: "commandResult", commandResult: CommandResult.decode(reader, reader.uint32()) };
          continue;
        }
        case 8: {
          if (tag !== 66) {
            break;
          }

          message.payload = { $case: "heartbeat", heartbeat: HeartBeat.decode(reader, reader.uint32()) };
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ConnectorToGatewayMessage {
    return {
      messageId: isSet(object.messageId)
        ? globalThis.String(object.messageId)
        : isSet(object.message_id)
        ? globalThis.String(object.message_id)
        : "",
      sentAt: isSet(object.sentAt)
        ? fromJsonTimestamp(object.sentAt)
        : isSet(object.sent_at)
        ? fromJsonTimestamp(object.sent_at)
        : undefined,
      payload: isSet(object.handshake)
        ? { $case: "handshake", handshake: ConnectorHandshake.fromJSON(object.handshake) }
        : isSet(object.propertyUpdate)
        ? { $case: "propertyUpdate", propertyUpdate: PropertyUpdateBatch.fromJSON(object.propertyUpdate) }
        : isSet(object.property_update)
        ? { $case: "propertyUpdate", propertyUpdate: PropertyUpdateBatch.fromJSON(object.property_update) }
        : isSet(object.deviceAnnounced)
        ? { $case: "deviceAnnounced", deviceAnnounced: DeviceAnnouncement.fromJSON(object.deviceAnnounced) }
        : isSet(object.device_announced)
        ? { $case: "deviceAnnounced", deviceAnnounced: DeviceAnnouncement.fromJSON(object.device_announced) }
        : isSet(object.deviceRemoved)
        ? { $case: "deviceRemoved", deviceRemoved: DeviceRemoval.fromJSON(object.deviceRemoved) }
        : isSet(object.device_removed)
        ? { $case: "deviceRemoved", deviceRemoved: DeviceRemoval.fromJSON(object.device_removed) }
        : isSet(object.commandResult)
        ? { $case: "commandResult", commandResult: CommandResult.fromJSON(object.commandResult) }
        : isSet(object.command_result)
        ? { $case: "commandResult", commandResult: CommandResult.fromJSON(object.command_result) }
        : isSet(object.heartbeat)
        ? { $case: "heartbeat", heartbeat: HeartBeat.fromJSON(object.heartbeat) }
        : undefined,
    };
  },

  toJSON(message: ConnectorToGatewayMessage): unknown {
    const obj: any = {};
    if (message.messageId !== undefined && message.messageId !== "") {
      obj.messageId = message.messageId;
    }
    if (message.sentAt !== undefined) {
      obj.sentAt = message.sentAt.toISOString();
    }
    if (message.payload?.$case === "handshake") {
      obj.handshake = ConnectorHandshake.toJSON(message.payload.handshake);
    } else if (message.payload?.$case === "propertyUpdate") {
      obj.propertyUpdate = PropertyUpdateBatch.toJSON(message.payload.propertyUpdate);
    } else if (message.payload?.$case === "deviceAnnounced") {
      obj.deviceAnnounced = DeviceAnnouncement.toJSON(message.payload.deviceAnnounced);
    } else if (message.payload?.$case === "deviceRemoved") {
      obj.deviceRemoved = DeviceRemoval.toJSON(message.payload.deviceRemoved);
    } else if (message.payload?.$case === "commandResult") {
      obj.commandResult = CommandResult.toJSON(message.payload.commandResult);
    } else if (message.payload?.$case === "heartbeat") {
      obj.heartbeat = HeartBeat.toJSON(message.payload.heartbeat);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ConnectorToGatewayMessage>, I>>(base?: I): ConnectorToGatewayMessage {
    return ConnectorToGatewayMessage.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ConnectorToGatewayMessage>, I>>(object: I): ConnectorToGatewayMessage {
    const message = createBaseConnectorToGatewayMessage();
    message.messageId = object.messageId ?? "";
    message.sentAt = object.sentAt ?? undefined;
    switch (object.payload?.$case) {
      case "handshake": {
        if (object.payload?.handshake !== undefined && object.payload?.handshake !== null) {
          message.payload = { $case: "handshake", handshake: ConnectorHandshake.fromPartial(object.payload.handshake) };
        }
        break;
      }
      case "propertyUpdate": {
        if (object.payload?.propertyUpdate !== undefined && object.payload?.propertyUpdate !== null) {
          message.payload = {
            $case: "propertyUpdate",
            propertyUpdate: PropertyUpdateBatch.fromPartial(object.payload.propertyUpdate),
          };
        }
        break;
      }
      case "deviceAnnounced": {
        if (object.payload?.deviceAnnounced !== undefined && object.payload?.deviceAnnounced !== null) {
          message.payload = {
            $case: "deviceAnnounced",
            deviceAnnounced: DeviceAnnouncement.fromPartial(object.payload.deviceAnnounced),
          };
        }
        break;
      }
      case "deviceRemoved": {
        if (object.payload?.deviceRemoved !== undefined && object.payload?.deviceRemoved !== null) {
          message.payload = {
            $case: "deviceRemoved",
            deviceRemoved: DeviceRemoval.fromPartial(object.payload.deviceRemoved),
          };
        }
        break;
      }
      case "commandResult": {
        if (object.payload?.commandResult !== undefined && object.payload?.commandResult !== null) {
          message.payload = {
            $case: "commandResult",
            commandResult: CommandResult.fromPartial(object.payload.commandResult),
          };
        }
        break;
      }
      case "heartbeat": {
        if (object.payload?.heartbeat !== undefined && object.payload?.heartbeat !== null) {
          message.payload = { $case: "heartbeat", heartbeat: HeartBeat.fromPartial(object.payload.heartbeat) };
        }
        break;
      }
    }
    return message;
  },
};

function createBaseGatewayToConnectorMessage(): GatewayToConnectorMessage {
  return { messageId: "", sentAt: undefined, payload: undefined };
}

export const GatewayToConnectorMessage: MessageFns<GatewayToConnectorMessage> = {
  encode(message: GatewayToConnectorMessage, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.messageId !== undefined && message.messageId !== "") {
      writer.uint32(10).string(message.messageId);
    }
    if (message.sentAt !== undefined) {
      Timestamp.encode(toTimestamp(message.sentAt), writer.uint32(18).fork()).join();
    }
    switch (message.payload?.$case) {
      case "handshakeAck":
        HandshakeAck.encode(message.payload.handshakeAck, writer.uint32(26).fork()).join();
        break;
      case "subscribe":
        SubscribeDevice.encode(message.payload.subscribe, writer.uint32(42).fork()).join();
        break;
      case "unsubscribe":
        UnsubscribeDevice.encode(message.payload.unsubscribe, writer.uint32(50).fork()).join();
        break;
      case "error":
        GatewayError.encode(message.payload.error, writer.uint32(58).fork()).join();
        break;
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GatewayToConnectorMessage {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGatewayToConnectorMessage();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.messageId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.sentAt = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.payload = { $case: "handshakeAck", handshakeAck: HandshakeAck.decode(reader, reader.uint32()) };
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.payload = { $case: "subscribe", subscribe: SubscribeDevice.decode(reader, reader.uint32()) };
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.payload = { $case: "unsubscribe", unsubscribe: UnsubscribeDevice.decode(reader, reader.uint32()) };
          continue;
        }
        case 7: {
          if (tag !== 58) {
            break;
          }

          message.payload = { $case: "error", error: GatewayError.decode(reader, reader.uint32()) };
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GatewayToConnectorMessage {
    return {
      messageId: isSet(object.messageId)
        ? globalThis.String(object.messageId)
        : isSet(object.message_id)
        ? globalThis.String(object.message_id)
        : "",
      sentAt: isSet(object.sentAt)
        ? fromJsonTimestamp(object.sentAt)
        : isSet(object.sent_at)
        ? fromJsonTimestamp(object.sent_at)
        : undefined,
      payload: isSet(object.handshakeAck)
        ? { $case: "handshakeAck", handshakeAck: HandshakeAck.fromJSON(object.handshakeAck) }
        : isSet(object.handshake_ack)
        ? { $case: "handshakeAck", handshakeAck: HandshakeAck.fromJSON(object.handshake_ack) }
        : isSet(object.subscribe)
        ? { $case: "subscribe", subscribe: SubscribeDevice.fromJSON(object.subscribe) }
        : isSet(object.unsubscribe)
        ? { $case: "unsubscribe", unsubscribe: UnsubscribeDevice.fromJSON(object.unsubscribe) }
        : isSet(object.error)
        ? { $case: "error", error: GatewayError.fromJSON(object.error) }
        : undefined,
    };
  },

  toJSON(message: GatewayToConnectorMessage): unknown {
    const obj: any = {};
    if (message.messageId !== undefined && message.messageId !== "") {
      obj.messageId = message.messageId;
    }
    if (message.sentAt !== undefined) {
      obj.sentAt = message.sentAt.toISOString();
    }
    if (message.payload?.$case === "handshakeAck") {
      obj.handshakeAck = HandshakeAck.toJSON(message.payload.handshakeAck);
    } else if (message.payload?.$case === "subscribe") {
      obj.subscribe = SubscribeDevice.toJSON(message.payload.subscribe);
    } else if (message.payload?.$case === "unsubscribe") {
      obj.unsubscribe = UnsubscribeDevice.toJSON(message.payload.unsubscribe);
    } else if (message.payload?.$case === "error") {
      obj.error = GatewayError.toJSON(message.payload.error);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GatewayToConnectorMessage>, I>>(base?: I): GatewayToConnectorMessage {
    return GatewayToConnectorMessage.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GatewayToConnectorMessage>, I>>(object: I): GatewayToConnectorMessage {
    const message = createBaseGatewayToConnectorMessage();
    message.messageId = object.messageId ?? "";
    message.sentAt = object.sentAt ?? undefined;
    switch (object.payload?.$case) {
      case "handshakeAck": {
        if (object.payload?.handshakeAck !== undefined && object.payload?.handshakeAck !== null) {
          message.payload = {
            $case: "handshakeAck",
            handshakeAck: HandshakeAck.fromPartial(object.payload.handshakeAck),
          };
        }
        break;
      }
      case "subscribe": {
        if (object.payload?.subscribe !== undefined && object.payload?.subscribe !== null) {
          message.payload = { $case: "subscribe", subscribe: SubscribeDevice.fromPartial(object.payload.subscribe) };
        }
        break;
      }
      case "unsubscribe": {
        if (object.payload?.unsubscribe !== undefined && object.payload?.unsubscribe !== null) {
          message.payload = {
            $case: "unsubscribe",
            unsubscribe: UnsubscribeDevice.fromPartial(object.payload.unsubscribe),
          };
        }
        break;
      }
      case "error": {
        if (object.payload?.error !== undefined && object.payload?.error !== null) {
          message.payload = { $case: "error", error: GatewayError.fromPartial(object.payload.error) };
        }
        break;
      }
    }
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends { $case: string } ? { [K in keyof Omit<T, "$case">]?: DeepPartial<T[K]> } & { $case: T["$case"] }
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function toTimestamp(date: Date): Timestamp {
  const seconds = Math.trunc(date.getTime() / 1_000);
  const nanos = (date.getTime() % 1_000) * 1_000_000;
  return { seconds, nanos };
}

function fromTimestamp(t: Timestamp): Date {
  let millis = (t.seconds || 0) * 1_000;
  millis += (t.nanos || 0) / 1_000_000;
  return new globalThis.Date(millis);
}

function fromJsonTimestamp(o: any): Date {
  if (o instanceof globalThis.Date) {
    return o;
  } else if (typeof o === "string") {
    return new globalThis.Date(o);
  } else {
    return fromTimestamp(Timestamp.fromJSON(o));
  }
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
