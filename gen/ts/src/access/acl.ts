// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.8
//   protoc               v5.27.3
// source: access/acl.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import { Timestamp } from "../google/protobuf/timestamp";
import { DeviceId, GatewayId, UserId } from "../identity/identity";
import {
  PermissionAction,
  permissionActionFromJSON,
  permissionActionToJSON,
  permissionActionToNumber,
  Role,
  roleFromJSON,
  roleToJSON,
  roleToNumber,
} from "./roles";

export const protobufPackage = "kusinta.iot.v1.access";

/**
 * PropertyConstraint bounds a single Matter cluster attribute.
 * attribute_name uses PascalCase Matter attribute naming (e.g. "MaxHeatSetpointLimit").
 * MaxHeatSetpointLimit and MinHeatSetpointLimit are actual Matter Thermostat cluster
 * attributes (0x0016, 0x0015) — the gateway writes them directly via the connector.
 */
export interface PropertyConstraint {
  attributeName?: string | undefined;
  constraint?:
    | { $case: "intMax"; intMax: number }
    | { $case: "intMin"; intMin: number }
    | { $case: "uintMax"; uintMax: number }
    | { $case: "uintMin"; uintMin: number }
    | undefined;
  clusterIdHex?: string | undefined;
}

export interface DeviceAcl {
  deviceId?: DeviceId | undefined;
  userId?: UserId | undefined;
  role?: Role | undefined;
  allowedActions?:
    | PermissionAction[]
    | undefined;
  /** empty = all attributes allowed */
  allowedAttributes?: string[] | undefined;
  propertyConstraints?: PropertyConstraint[] | undefined;
}

export interface EffectivePermissions {
  userId?: UserId | undefined;
  gatewayId?: GatewayId | undefined;
  deviceAcls?: DeviceAcl[] | undefined;
  validAt?: Date | undefined;
}

function createBasePropertyConstraint(): PropertyConstraint {
  return { attributeName: "", constraint: undefined, clusterIdHex: "" };
}

export const PropertyConstraint: MessageFns<PropertyConstraint> = {
  encode(message: PropertyConstraint, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.attributeName !== undefined && message.attributeName !== "") {
      writer.uint32(10).string(message.attributeName);
    }
    switch (message.constraint?.$case) {
      case "intMax":
        writer.uint32(16).sint32(message.constraint.intMax);
        break;
      case "intMin":
        writer.uint32(24).sint32(message.constraint.intMin);
        break;
      case "uintMax":
        writer.uint32(32).uint32(message.constraint.uintMax);
        break;
      case "uintMin":
        writer.uint32(40).uint32(message.constraint.uintMin);
        break;
    }
    if (message.clusterIdHex !== undefined && message.clusterIdHex !== "") {
      writer.uint32(50).string(message.clusterIdHex);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): PropertyConstraint {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBasePropertyConstraint();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.attributeName = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.constraint = { $case: "intMax", intMax: reader.sint32() };
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.constraint = { $case: "intMin", intMin: reader.sint32() };
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.constraint = { $case: "uintMax", uintMax: reader.uint32() };
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.constraint = { $case: "uintMin", uintMin: reader.uint32() };
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.clusterIdHex = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): PropertyConstraint {
    return {
      attributeName: isSet(object.attributeName)
        ? globalThis.String(object.attributeName)
        : isSet(object.attribute_name)
        ? globalThis.String(object.attribute_name)
        : "",
      constraint: isSet(object.intMax)
        ? { $case: "intMax", intMax: globalThis.Number(object.intMax) }
        : isSet(object.int_max)
        ? { $case: "intMax", intMax: globalThis.Number(object.int_max) }
        : isSet(object.intMin)
        ? { $case: "intMin", intMin: globalThis.Number(object.intMin) }
        : isSet(object.int_min)
        ? { $case: "intMin", intMin: globalThis.Number(object.int_min) }
        : isSet(object.uintMax)
        ? { $case: "uintMax", uintMax: globalThis.Number(object.uintMax) }
        : isSet(object.uint_max)
        ? { $case: "uintMax", uintMax: globalThis.Number(object.uint_max) }
        : isSet(object.uintMin)
        ? { $case: "uintMin", uintMin: globalThis.Number(object.uintMin) }
        : isSet(object.uint_min)
        ? { $case: "uintMin", uintMin: globalThis.Number(object.uint_min) }
        : undefined,
      clusterIdHex: isSet(object.clusterIdHex)
        ? globalThis.String(object.clusterIdHex)
        : isSet(object.cluster_id_hex)
        ? globalThis.String(object.cluster_id_hex)
        : "",
    };
  },

  toJSON(message: PropertyConstraint): unknown {
    const obj: any = {};
    if (message.attributeName !== undefined && message.attributeName !== "") {
      obj.attributeName = message.attributeName;
    }
    if (message.constraint?.$case === "intMax") {
      obj.intMax = Math.round(message.constraint.intMax);
    } else if (message.constraint?.$case === "intMin") {
      obj.intMin = Math.round(message.constraint.intMin);
    } else if (message.constraint?.$case === "uintMax") {
      obj.uintMax = Math.round(message.constraint.uintMax);
    } else if (message.constraint?.$case === "uintMin") {
      obj.uintMin = Math.round(message.constraint.uintMin);
    }
    if (message.clusterIdHex !== undefined && message.clusterIdHex !== "") {
      obj.clusterIdHex = message.clusterIdHex;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<PropertyConstraint>, I>>(base?: I): PropertyConstraint {
    return PropertyConstraint.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<PropertyConstraint>, I>>(object: I): PropertyConstraint {
    const message = createBasePropertyConstraint();
    message.attributeName = object.attributeName ?? "";
    switch (object.constraint?.$case) {
      case "intMax": {
        if (object.constraint?.intMax !== undefined && object.constraint?.intMax !== null) {
          message.constraint = { $case: "intMax", intMax: object.constraint.intMax };
        }
        break;
      }
      case "intMin": {
        if (object.constraint?.intMin !== undefined && object.constraint?.intMin !== null) {
          message.constraint = { $case: "intMin", intMin: object.constraint.intMin };
        }
        break;
      }
      case "uintMax": {
        if (object.constraint?.uintMax !== undefined && object.constraint?.uintMax !== null) {
          message.constraint = { $case: "uintMax", uintMax: object.constraint.uintMax };
        }
        break;
      }
      case "uintMin": {
        if (object.constraint?.uintMin !== undefined && object.constraint?.uintMin !== null) {
          message.constraint = { $case: "uintMin", uintMin: object.constraint.uintMin };
        }
        break;
      }
    }
    message.clusterIdHex = object.clusterIdHex ?? "";
    return message;
  },
};

function createBaseDeviceAcl(): DeviceAcl {
  return {
    deviceId: undefined,
    userId: undefined,
    role: Role.ROLE_UNSPECIFIED,
    allowedActions: [],
    allowedAttributes: [],
    propertyConstraints: [],
  };
}

export const DeviceAcl: MessageFns<DeviceAcl> = {
  encode(message: DeviceAcl, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.deviceId !== undefined) {
      DeviceId.encode(message.deviceId, writer.uint32(10).fork()).join();
    }
    if (message.userId !== undefined) {
      UserId.encode(message.userId, writer.uint32(18).fork()).join();
    }
    if (message.role !== undefined && message.role !== Role.ROLE_UNSPECIFIED) {
      writer.uint32(24).int32(roleToNumber(message.role));
    }
    if (message.allowedActions !== undefined && message.allowedActions.length !== 0) {
      writer.uint32(34).fork();
      for (const v of message.allowedActions) {
        writer.int32(permissionActionToNumber(v));
      }
      writer.join();
    }
    if (message.allowedAttributes !== undefined && message.allowedAttributes.length !== 0) {
      for (const v of message.allowedAttributes) {
        writer.uint32(42).string(v!);
      }
    }
    if (message.propertyConstraints !== undefined && message.propertyConstraints.length !== 0) {
      for (const v of message.propertyConstraints) {
        PropertyConstraint.encode(v!, writer.uint32(50).fork()).join();
      }
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): DeviceAcl {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseDeviceAcl();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.deviceId = DeviceId.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.userId = UserId.decode(reader, reader.uint32());
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.role = roleFromJSON(reader.int32());
          continue;
        }
        case 4: {
          if (tag === 32) {
            message.allowedActions!.push(permissionActionFromJSON(reader.int32()));

            continue;
          }

          if (tag === 34) {
            const end2 = reader.uint32() + reader.pos;
            while (reader.pos < end2) {
              message.allowedActions!.push(permissionActionFromJSON(reader.int32()));
            }

            continue;
          }

          break;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          const el = reader.string();
          if (el !== undefined) {
            message.allowedAttributes!.push(el);
          }
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          const el = PropertyConstraint.decode(reader, reader.uint32());
          if (el !== undefined) {
            message.propertyConstraints!.push(el);
          }
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): DeviceAcl {
    return {
      deviceId: isSet(object.deviceId)
        ? DeviceId.fromJSON(object.deviceId)
        : isSet(object.device_id)
        ? DeviceId.fromJSON(object.device_id)
        : undefined,
      userId: isSet(object.userId)
        ? UserId.fromJSON(object.userId)
        : isSet(object.user_id)
        ? UserId.fromJSON(object.user_id)
        : undefined,
      role: isSet(object.role) ? roleFromJSON(object.role) : Role.ROLE_UNSPECIFIED,
      allowedActions: globalThis.Array.isArray(object?.allowedActions)
        ? object.allowedActions.map((e: any) => permissionActionFromJSON(e))
        : globalThis.Array.isArray(object?.allowed_actions)
        ? object.allowed_actions.map((e: any) => permissionActionFromJSON(e))
        : [],
      allowedAttributes: globalThis.Array.isArray(object?.allowedAttributes)
        ? object.allowedAttributes.map((e: any) => globalThis.String(e))
        : globalThis.Array.isArray(object?.allowed_attributes)
        ? object.allowed_attributes.map((e: any) => globalThis.String(e))
        : [],
      propertyConstraints: globalThis.Array.isArray(object?.propertyConstraints)
        ? object.propertyConstraints.map((e: any) => PropertyConstraint.fromJSON(e))
        : globalThis.Array.isArray(object?.property_constraints)
        ? object.property_constraints.map((e: any) => PropertyConstraint.fromJSON(e))
        : [],
    };
  },

  toJSON(message: DeviceAcl): unknown {
    const obj: any = {};
    if (message.deviceId !== undefined) {
      obj.deviceId = DeviceId.toJSON(message.deviceId);
    }
    if (message.userId !== undefined) {
      obj.userId = UserId.toJSON(message.userId);
    }
    if (message.role !== undefined && message.role !== Role.ROLE_UNSPECIFIED) {
      obj.role = roleToJSON(message.role);
    }
    if (message.allowedActions?.length) {
      obj.allowedActions = message.allowedActions.map((e) => permissionActionToJSON(e));
    }
    if (message.allowedAttributes?.length) {
      obj.allowedAttributes = message.allowedAttributes;
    }
    if (message.propertyConstraints?.length) {
      obj.propertyConstraints = message.propertyConstraints.map((e) => PropertyConstraint.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<DeviceAcl>, I>>(base?: I): DeviceAcl {
    return DeviceAcl.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<DeviceAcl>, I>>(object: I): DeviceAcl {
    const message = createBaseDeviceAcl();
    message.deviceId = (object.deviceId !== undefined && object.deviceId !== null)
      ? DeviceId.fromPartial(object.deviceId)
      : undefined;
    message.userId = (object.userId !== undefined && object.userId !== null)
      ? UserId.fromPartial(object.userId)
      : undefined;
    message.role = object.role ?? Role.ROLE_UNSPECIFIED;
    message.allowedActions = object.allowedActions?.map((e) => e) || [];
    message.allowedAttributes = object.allowedAttributes?.map((e) => e) || [];
    message.propertyConstraints = object.propertyConstraints?.map((e) => PropertyConstraint.fromPartial(e)) || [];
    return message;
  },
};

function createBaseEffectivePermissions(): EffectivePermissions {
  return { userId: undefined, gatewayId: undefined, deviceAcls: [], validAt: undefined };
}

export const EffectivePermissions: MessageFns<EffectivePermissions> = {
  encode(message: EffectivePermissions, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.userId !== undefined) {
      UserId.encode(message.userId, writer.uint32(10).fork()).join();
    }
    if (message.gatewayId !== undefined) {
      GatewayId.encode(message.gatewayId, writer.uint32(18).fork()).join();
    }
    if (message.deviceAcls !== undefined && message.deviceAcls.length !== 0) {
      for (const v of message.deviceAcls) {
        DeviceAcl.encode(v!, writer.uint32(26).fork()).join();
      }
    }
    if (message.validAt !== undefined) {
      Timestamp.encode(toTimestamp(message.validAt), writer.uint32(34).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): EffectivePermissions {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseEffectivePermissions();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.userId = UserId.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.gatewayId = GatewayId.decode(reader, reader.uint32());
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          const el = DeviceAcl.decode(reader, reader.uint32());
          if (el !== undefined) {
            message.deviceAcls!.push(el);
          }
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.validAt = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): EffectivePermissions {
    return {
      userId: isSet(object.userId)
        ? UserId.fromJSON(object.userId)
        : isSet(object.user_id)
        ? UserId.fromJSON(object.user_id)
        : undefined,
      gatewayId: isSet(object.gatewayId)
        ? GatewayId.fromJSON(object.gatewayId)
        : isSet(object.gateway_id)
        ? GatewayId.fromJSON(object.gateway_id)
        : undefined,
      deviceAcls: globalThis.Array.isArray(object?.deviceAcls)
        ? object.deviceAcls.map((e: any) => DeviceAcl.fromJSON(e))
        : globalThis.Array.isArray(object?.device_acls)
        ? object.device_acls.map((e: any) => DeviceAcl.fromJSON(e))
        : [],
      validAt: isSet(object.validAt)
        ? fromJsonTimestamp(object.validAt)
        : isSet(object.valid_at)
        ? fromJsonTimestamp(object.valid_at)
        : undefined,
    };
  },

  toJSON(message: EffectivePermissions): unknown {
    const obj: any = {};
    if (message.userId !== undefined) {
      obj.userId = UserId.toJSON(message.userId);
    }
    if (message.gatewayId !== undefined) {
      obj.gatewayId = GatewayId.toJSON(message.gatewayId);
    }
    if (message.deviceAcls?.length) {
      obj.deviceAcls = message.deviceAcls.map((e) => DeviceAcl.toJSON(e));
    }
    if (message.validAt !== undefined) {
      obj.validAt = message.validAt.toISOString();
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<EffectivePermissions>, I>>(base?: I): EffectivePermissions {
    return EffectivePermissions.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<EffectivePermissions>, I>>(object: I): EffectivePermissions {
    const message = createBaseEffectivePermissions();
    message.userId = (object.userId !== undefined && object.userId !== null)
      ? UserId.fromPartial(object.userId)
      : undefined;
    message.gatewayId = (object.gatewayId !== undefined && object.gatewayId !== null)
      ? GatewayId.fromPartial(object.gatewayId)
      : undefined;
    message.deviceAcls = object.deviceAcls?.map((e) => DeviceAcl.fromPartial(e)) || [];
    message.validAt = object.validAt ?? undefined;
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends { $case: string } ? { [K in keyof Omit<T, "$case">]?: DeepPartial<T[K]> } & { $case: T["$case"] }
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function toTimestamp(date: Date): Timestamp {
  const seconds = Math.trunc(date.getTime() / 1_000);
  const nanos = (date.getTime() % 1_000) * 1_000_000;
  return { seconds, nanos };
}

function fromTimestamp(t: Timestamp): Date {
  let millis = (t.seconds || 0) * 1_000;
  millis += (t.nanos || 0) / 1_000_000;
  return new globalThis.Date(millis);
}

function fromJsonTimestamp(o: any): Date {
  if (o instanceof globalThis.Date) {
    return o;
  } else if (typeof o === "string") {
    return new globalThis.Date(o);
  } else {
    return fromTimestamp(Timestamp.fromJSON(o));
  }
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
